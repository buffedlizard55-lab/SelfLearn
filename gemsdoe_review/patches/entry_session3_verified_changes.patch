From 3440566ddf61bd22a1402b125f2949af588c226c Mon Sep 17 00:00:00 2001
From: "arena-ai-coding-agent[bot]"
 <298482267+arena-ai-coding-agent[bot]@users.noreply.github.com>
Date: Sat, 26 Sep 2026 17:02:21 +0000
Subject: [PATCH 1/2] Apply the review's CV-buffer fix and entry-docs
 corrections; add per-candidate geology dossier

- src/gems/cv.py: _dilate is now a true Euclidean disk (was a 4-neighbour
  Manhattan diamond that missed the (2,2) corner at sqrt(8) px = 283 m,
  inside the official 300 m kernel). Historical 4x4/6x6 stripe layouts
  measured zero misses, so no retrospective score changes; the fix repairs
  the general buffered-CV contract (5x5/3-fold had 32, 6x6/4-fold had 100).
- tests: replace the always-true 'assert ... or True' with an independent
  distance-transform oracle, and add the minimal diagonal-corner case that
  fails on the old code and passes on the new one (verified both ways).
- LIMITATIONS.md: state the staff-clarified masking rule (only supplied
  training-label pixels are excluded; 1-3 px off-trace mass is NOT exempt);
  record the measured tilt-derivative audit (dead mag_tilt, mag_asa ~
  tmi_hg duplicate); separate the provable uniform-scaling identity from
  the empirical hard-1.0 policy.
- EXECUTIVE_SUMMARY.md / SUBMISSION_GUIDE.md / site: put the account-holder
  checks BEFORE any download/upload instruction; 'true score will be lower'
  -> 'hidden score is unknown'; 'weaker than a U-Net' -> 'not benchmarked';
  fix the 'fractional confidence gives score away' overgeneralization in
  the suggested comment and the experiments/research cards; qualify the
  'dilation loses' slogan (catalogue-proxy statement, SGMC study failed its
  no-skill gate).
- NEXT_STEPS.md: remove the obsolete bridge/codeload warning (the bridge is
  first-party in data/bridge/ since 2026-09-26).
- scripts/build_features.py: fix the stale '48 channels ~2.25 GB' comment
  (88 channels = 4,322,264,320 bytes).
- data/evidence/geology_dossier.md: per-candidate geological reasoning for
  all 180 flagged structures of the shipped file (NEXT_STEPS item 9),
  generated from the measured dossier on the pinned bytes.

Gate 13/13 PASS re-verified; pytest 47/47; check_entry_docs 50/50 (was
48/50: masking rule + geology document were the two gaps).
---
 EXECUTIVE_SUMMARY.md             |  48 +-
 LIMITATIONS.md                   |  42 +-
 NEXT_STEPS.md                    |  10 +-
 SUBMISSION_GUIDE.md              |  27 +-
 data/evidence/geology_dossier.md | 951 +++++++++++++++++++++++++++++++
 index.html                       |  25 +-
 research.html                    |  20 +-
 scripts/build_features.py        |   6 +-
 scripts/build_site.py            |  45 +-
 src/gems/cv.py                   |  27 +-
 tests/test_spec_and_cv.py        |  38 +-
 11 files changed, 1173 insertions(+), 66 deletions(-)
 create mode 100644 data/evidence/geology_dossier.md

diff --git a/EXECUTIVE_SUMMARY.md b/EXECUTIVE_SUMMARY.md
index b05d07a..a80907a 100644
--- a/EXECUTIVE_SUMMARY.md
+++ b/EXECUTIVE_SUMMARY.md
@@ -3,7 +3,17 @@
 **The whole point of this repository is the one file you upload.** This page is the
 shortest path to it, followed by what has and has not been verified.
 
-## 1. Get the file (one download, one upload)
+## 1. Before any upload (account holder, with credentials)
+
+0. **Confirm the one DrivenData entry** this repository speaks for, the §1.3
+   eligibility of the registered competitor, and this week's remaining submission
+   allowance (three per week, §3.4) by reading *that account's* submission history.
+   Confirm the §3.2 generative-AI disclosure is ready (`NARRATIVES.md` §2). This
+   sandbox has no DrivenData session, so none of those facts can be checked from
+   here — they are human-owned and deliberately left blank in
+   `SUBMISSION_GUIDE.md`'s one-entry record.
+
+## 2. Get the file (one download, one upload)
 
 1. **Download** the submission GeoTIFF from the published site
    (<https://buffedlizard55-lab.github.io/6GEMSDOE/>) — the download button is the
@@ -24,7 +34,7 @@ shortest path to it, followed by what has and has not been verified.
 exactly one final submission across both rounds (§3.5, §3.6.2). Do not spend a slot
 until the file has passed `scripts/validate_submission.py` locally.
 
-## 2. What the file must satisfy (all rules quoted from the official pages)
+## 3. What the file must satisfy (all rules quoted from the official pages)
 
 Re-fetched and re-checked on 2026-09-25 and **re-verified 2026-09-26 00:20 UTC, 01:19 UTC, and again 04:25 UTC** from
 <https://www.drivendata.org/competitions/306/competition-doe-gems/page/967/> (both chunks fetched via platform fetcher; submission format, metric, and worked example re-confirmed; gate 13/13 PASS, hashes 3/3 OK — `data/evidence/session_reverification_2026-09-26T0425Z.json`).
@@ -39,7 +49,7 @@ Re-fetched and re-checked on 2026-09-25 and **re-verified 2026-09-26 00:20 UTC,
 | Values | between 0 and 1, higher = higher fault probability | same |
 | Size | one GeoTIFF for the whole GeoDAWN study area | rules §3.2 |
 
-## 3. What the shipped file is, and what it measured
+## 4. What the shipped file is, and what it measured
 
 Built by
 
@@ -79,8 +89,12 @@ DTI = TP_w / ( beta * n_gt + alpha * FP_w + (1 - beta) * TP_w )
 ```
 
 and the `beta * n_gt` term does **not** scale with the predicted probability, so DTI
-is strictly increasing under `p → λ·p` for any λ up to the cap at 1. A submission
-that carries fractional confidence values is donating score to nobody.
+is strictly increasing under `p → λ·p` — **uniform rescaling of every predicted
+value** — for any λ up to the cap at 1. A submission that carries uniformly shrunk
+confidence values is donating score to nobody. Hardening each selected value to 1
+is a separate, *empirical* catalogue policy (it measured +36–49% here); it is not a
+theorem that every fractional value should be 1, because a high-confidence remote
+false positive is charged the full `α` either way.
 
 The second change is a budget choice. `FP_w` is an absolute sum over predicted
 pixels while `TP_w`/`FN_w` are per-ground-truth-pixel sums, so the right amount to
@@ -129,7 +143,7 @@ in the worst.
   examples of the same physics, not a different target — down-weighting them
   removes the anchor of the probability surface.
 
-## 4. What is verified in this repository
+## 5. What is verified in this repository
 
 * **The official bytes are hash-verified.** `data/training_features.tif`
   (418,912,844 B, sha256 `4371c82e…`), `data/labels.tif` (425,830 B, sha256
@@ -154,7 +168,7 @@ in the worst.
   identical to the value the previous submission was chosen on. So the improvements
   above are measured against the same yardstick, not a new one.
 
-## 5. What is NOT claimed
+## 6. What is NOT claimed
 
 * **No leaderboard score is claimed or predicted.** The competition is scored against
   private expert labels; we do not have them, and we have not uploaded anything.
@@ -164,15 +178,25 @@ in the worst.
   MISSING from the catalogue (the competition home page confirms the test set is
   "newly identified faults" with spatial overlap to the training faults). Our proxy
   measures rediscovery of *known* faults from held-out blocks, which is easier than
-  the real task. The true score will be lower than 0.1698; we do not know by how
-  much.
+  the real task. **The hidden score is unknown** — it is not predicted by 0.1698 in
+  either direction, and staff clarification says only the supplied training-label
+  pixels are masked out in scoring (see `LIMITATIONS.md` §1).
 * **The model here is not the reference solution's model.** The reference is a
   ResNet-18 U-Net trained over five Monte-Carlo folds. This host has 2 CPUs, 4 GB of
   RAM and no GPU, so the shipped model is a histogram gradient-boosting classifier on
-  sampled pixels over 88 feature channels. It is weaker, and it is labelled as such.
+  sampled pixels over 88 feature channels. The two architectures were **not
+  benchmarked against each other on the hidden target**, so no "weaker/stronger"
+  claim is made — only that this one is reproducible on this host and measured on
+  blocked folds.
 * **The 1 m DEM is not incorporated.** See `LIMITATIONS.md`.
-
-## 6. The single outstanding risk to eligibility
+* **The supplied magnetic tilt channel is not evidence.** The shipped stack keeps
+  `mag_tilt` and `mag_asa` as passthrough columns, but the independent re-audit
+  recorded in `LIMITATIONS.md` §5 measured the supplied tilt as numerically dead
+  (p99 |angle| ≈ 3.1°) and `mag_asa` as a near-duplicate of `tmi_hg`
+  (r = 1.000000). No depth, dip or magnetic-edge claim in this repo relies on
+  them.
+
+## 7. The single outstanding risk to eligibility
 
 The hosting account holds **eleven** GEMS-named repositories and has **GitHub Pages
 enabled on all eleven** for this one challenge — five of them complete copies with
diff --git a/LIMITATIONS.md b/LIMITATIONS.md
index 70baee8..6231280 100644
--- a/LIMITATIONS.md
+++ b/LIMITATIONS.md
@@ -19,6 +19,19 @@ repo is therefore labelled a proxy, and the blocked-CV tables report the *gap* c
 **Consequence:** we cannot know whether the shipped file is competitive. We can only
 know that it is well-formed, reproducible, and built on the right target.
 
+**What is actually masked from scoring (staff clarification, 2026-09-26).**
+DrivenData staff say *only the supplied training-label pixels* are masked out of
+evaluation, in **both** phases ([community posts 2 and 4](https://community.drivendata.org/t/11516/4)).
+Three consequences, stated because earlier drafts of this repo assumed a wider
+exclusion:
+
+* predictions 1–3 px from a catalogued trace are **not** exempt from false-positive
+  cost — unless they happen to sit near *new-fault* truth;
+* the new (hidden) truth may itself include corrections **within 300 m of mapped
+  faults**, i.e. near-catalogue space is not automatically dead space;
+* our catalogue blocked-CV DTI is not a score on that population — it never sees
+  the masking rule applied to a new-fault ground truth.
+
 ## 2. No leaderboard feedback is available from here
 
 A feedback channel *exists* — the competition splits the new-fault set into a public
@@ -71,6 +84,24 @@ information. `scripts/analysis.py --only bands` measures this empirically (corre
 and median absolute difference per candidate pair) so the redundancy is documented
 rather than assumed.
 
+**Measured on the official bytes (2026-09-26, independent re-audit).** The
+measurements live in the companion review repository
+[`buffedlizard55-lab/SelfLearn`](https://github.com/buffedlizard55-lab/SelfLearn):
+`gemsdoe_review/evidence/tilt_derivative_audit.json` and
+`gemsdoe_review/evidence/feature_priorities_audit_2026-09-26.{json,md}`, computed
+from the sha256-pinned rasters. The supplied
+`mag_tilt` channel is **numerically dead** as a tilt: its 99th-percentile absolute
+angle is ~3.1° and it reaches ±45° on ~2×10⁻⁵ of pixels, so it cannot support the
+±45° half-width contours the tilt-depth method needs; the supplied `tmi_vg` is about
+256× smaller than `tmi_hg`, which is a unit/scale mismatch, not a physical
+near-zero field. The derived `mag_asa` is a near-duplicate of the supplied `tmi_hg`
+(Pearson r = 1.000000). The *smoothed* magnetic tilts (`tdr_tmi_s1.5/s3.0`) are
+angle-active (median ≈ 40°) but carry no catalogue separation (AUC 0.4998 / 0.4987
+against mapped-fault pixels), and the gravity tilt is mildly anti-separated (AUC
+0.4843). **Consequence:** no magnetic-tilt variant in this stack should be read as a
+depth or dip indicator, and Phase-2 narratives must not cite magnetic tilt as
+independent evidence. The gravity tilt remains a useful *descriptive* anomaly only.
+
 **Measured consequence (2026-09-25).** Appending forty genuinely new multi-scale
 channels — horizontal-gradient magnitude at σ = 1.5/3/6 px on five surfaces,
 analytic-signal amplitude and tilt derivative at matched scales, multi-scale
@@ -103,9 +134,14 @@ spacing) is metric algebra and holds regardless.
 The value and budget results are stronger than that, because they are algebra plus
 measurement rather than measurement alone:
 
-* writing `1.0` instead of the model probability is *provably* better (DTI is strictly
-  increasing under `p → λ·p`, because the `β·n_gt` term does not scale), and it
-  measured +36% to +49% depending on the threshold;
+* writing `1.0` on the *selected* pixels instead of the model probability measured
+  +36% to +49% depending on the threshold. What is *provably* better is the narrower
+  uniform-scaling statement: DTI is strictly increasing under `p → λ·p` applied to
+  **every** predicted value at once (the `β·n_gt` term does not scale), so a
+  uniformly shrunk submission is donating score. Hardening *each* value to 1 while
+  the selection stays fixed is an empirical catalogue policy that happened to win
+  here, not a theorem: on uncertain pixels a high-confidence false positive is
+  charged the full `α` whether it was 0.01 or 1.0 in the soft surface;
 * the budget choice is hedged against the unknown size of the private test set by
   re-scoring identical predictions against ground truth thinned to whole fault traces.
   The 3% budget is the minimax-regret choice on two independent blockings (4×4 and
diff --git a/NEXT_STEPS.md b/NEXT_STEPS.md
index 230bc00..d7751de 100644
--- a/NEXT_STEPS.md
+++ b/NEXT_STEPS.md
@@ -14,10 +14,12 @@ quality against the *unmapped-fault* target, and the compliance items.
    An ineligible winner is disqualified regardless of the model. Owner: the account
    holder. Nothing in this repo can check it.
 2. **Resolve the duplication flag in `ACCOUNT_STATUS.md`.** One entry, one repo, one
-   site. Before archiving the other ten, first replace the data path: the pinned bridge
-   currently resolves through `buffedlizard55-lab/GEMSDOE` over codeload. Either copy
-   the bridge parts into this repo or download the three files from the official data
-   tab while signed in — then archive the duplicates.
+   site. The data path is no longer a blocker: the bridge parts are committed
+   first-party in `data/bridge/` (sha256-pinned in `data/bridge/manifest.json` and
+   re-verified in `src/gems/spec.py`), so this repository no longer depends on
+   `buffedlizard55-lab/GEMSDOE` or on codeload. (That older instruction is obsolete.)
+   With the data self-contained, archive the duplicate sibling repositories only
+   after the account holder confirms nothing else needs them.
 3. **Record the state of the weekly submission allowance** (three per week, §3.4) once
    somebody with credentials can read the submission history, and write the chosen
    final submission into `SUBMISSION_GUIDE.md`.
diff --git a/SUBMISSION_GUIDE.md b/SUBMISSION_GUIDE.md
index 0a3f3d8..8fd23e9 100644
--- a/SUBMISSION_GUIDE.md
+++ b/SUBMISSION_GUIDE.md
@@ -1,5 +1,26 @@
 # Submission guide and the one-entry record
 
+## 0. Before anything else (account holder, with credentials)
+
+Before **any** upload — and before treating any number in this repo as a decision —
+
+1. confirm **the one DrivenData registration** this entry corresponds to, and read
+   *that account's* submission history (has anything ever been uploaded?);
+2. confirm the registered competitor's **eligibility** (rules §1.3: U.S. citizen or
+   permanent resident, or a U.S.-incorporated entity with such a captain; not a
+   Federal employee, not under 18, not in a Malign Foreign Talent Recruitment
+   Program);
+3. confirm the **remaining weekly allowance** (three scored submissions per week,
+   §3.4);
+4. confirm the **§3.2 generative-AI disclosure** is written (draft:
+   `NARRATIVES.md` §2);
+5. re-run the format gate on the exact bytes selected (command below). A local
+   proxy score is not a reason to upload; it is context for the decision.
+
+None of those facts are knowable from this sandbox: it has no DrivenData session.
+They are recorded — blank — in the one-entry record at the end of this page, and
+they must be filled in by the account holder, never guessed.
+
 ## The two-click path
 
 1. **Download** `downloads/gems6_hgb88-topk03_33cec71ff0.tif` (the link is the first
@@ -67,8 +88,10 @@ candidate, not a batch):
 > 400k sampled negatives, validated on spatially blocked, buffered folds (300 m
 > buffer). The submission keeps the top 3% of the footprint by predicted probability
 > and writes 1.0 on those pixels: the published metric reduces to
-> DTI = TP_w/(0.8·n_gt + 0.2·FP_w + 0.2·TP_w), which is strictly increasing in the
-> predicted value, so fractional confidence gives score away. The budget is the
+> DTI = TP_w/(0.8·n_gt + 0.2·FP_w + 0.2·TP_w), which is strictly increasing under
+> uniform rescaling of every predicted value, and the hard 1.0 mask on the selected
+> 3% was the measured winner on blocked folds (an empirical catalogue policy, not a
+> claim that every fractional value should be 1). The budget is the
 > minimax-regret choice across ground-truth sizes (worst-case loss 5% vs 13% for the
 > 5% budget that leads on the full catalogue). Blocked-CV proxy DTI 0.1698 against
 > the public catalogue — a proxy, not a leaderboard value. Format verified by
diff --git a/data/evidence/geology_dossier.md b/data/evidence/geology_dossier.md
new file mode 100644
index 0000000..93e30ab
--- /dev/null
+++ b/data/evidence/geology_dossier.md
@@ -0,0 +1,951 @@
+# Per-candidate geological dossier — the shipped file (NEXT_STEPS item 9)
+
+This is the entry's per-candidate geology document. It was generated in the
+companion review repository `buffedlizard55-lab/SelfLearn`
+(`gemsdoe_review/tools/phase2_narrative.py`, session of 2026-09-26) from the
+measured dossier `current_shipped_geology_2026-09-26.json`, which was computed
+from the **official pinned rasters** and the **exact shipped submission bytes**
+(SHA-256 `33cec71ff00b3f32d0d59c81c156f3f1488ffef46baa4b6499094e24ea1875ab`).
+Every number below is a measurement of those bytes; the geological readings are
+hand-written interpretations of measured fields, each with its
+counterinterpretation and a confidence tier capped at "provisional".
+
+Read it together with `LIMITATIONS.md` (esp. §1 on the masking rule and §5 on why
+the supplied magnetic tilt cannot vote as evidence) and `CANDIDATES.md` (the
+machine-generated geometry table for the same file).
+
+Per-candidate geological reasoning for every >=200-closed-px group the model
+flags in the **shipped** submission (SHA-256 `33cec71ff00b3f32d0d59c81c156f3f1488ffef46baa4b6499094e24ea1875ab`, first 12: `33cec71ff00b`),
+rendered by `tools/phase2_narrative.py` from the measured dossier
+`current_shipped_geology_2026-09-26.json`. The mask is a hypothesis list, not
+a fault map.
+
+**Regional frame (cited sources only).** The GeoDAWN survey area covers
+ground "associated with the Walker Lane and western Great Basin"
+([competition About page](https://www.drivendata.org/competitions/306/competition-doe-gems/page/968/)).
+The Walker Lane is documented as a belt of northwest-trending, right-lateral
+shear ([USGS DDS-58 ch. I](https://pubs.usgs.gov/dds/dds-058/Ch_I.pdf));
+Basin-and-Range extension in Nevada is expressed as northerly-trending
+high-angle normal faults forming north-trending basins and ranges
+([USGS OFR 67-11](https://pubs.usgs.gov/publication/ofr6711);
+[USGS FAQ on normal faults](https://www.usgs.gov/faqs/what-a-fault-and-what-are-different-types)).
+These sources justify using NW ~300-345 deg, N ~345-25 deg and NE ~25-70 deg
+as *interpretation aids* for measured azimuths. They do not identify any
+specific structure.
+
+**What every entry below is:** a measured geometry + measured signal-decile
+support + measured distance class + the depth status, composed into a
+hypothesis with its counterinterpretation. **What none of them is:** a
+verified fault, a depth estimate, or a prediction of hidden score. Tilt-depth
+is not estimable on the supplied bands (dead magnetic tilt); where this file
+says a group is 'isolated', isolation is a distance heuristic, not a
+discovery. Under the staff masking rule, mass within 300 m of the training
+labels is not exempt from false-positive cost.
+
+**Measured headline for this file:** 180 candidates; agreement histogram {0: 66, 1: 81, 2: 27, 3: 6} (families of 6) — only 6 of 180 reach 3/6 families, and **no candidate reaches the 'provisional' tier** (needs >=3 families AND elongation >= 2 AND >= 1,000 emitted px). The shipped flags are, at group level, weakly supported; treat the whole file as a broad anomaly surface, not a line list.
+
+## 180 candidates, largest first
+
+| # | id | class | conf | fam/6 | az | elong | emitted px |
+| --- | --- | --- | --- | --- | --- | --- | ---: |
+| 1 | 2353 | near_trace | low-to-provisional | 2 | 98 | 3.36 | 11,580 |
+| 2 | 2460 | near_trace | low-to-provisional | 1 | 149 | 9.80 | 8,914 |
+| 3 | 2217 | near_trace | low-to-provisional | 1 | 175 | 1.77 | 5,397 |
+| 4 | 1293 | halo | low-to-provisional | 1 | 12 | 5.24 | 4,596 |
+| 5 | 1213 | near_trace | low | 0 | 17 | 1.99 | 4,061 |
+| 6 | 292 | near_trace | low-to-provisional | 1 | 13 | 2.00 | 2,486 |
+| 7 | 2419 | near_trace | low-to-provisional | 1 | 23 | 2.89 | 2,271 |
+| 8 | 2360 | near_trace | low-to-provisional | 1 | 46 | 3.81 | 1,761 |
+| 9 | 1578 | near_trace | low | 0 | 79 | 2.58 | 1,751 |
+| 10 | 795 | near_trace | low-to-provisional | 1 | 73 | 1.10 | 1,575 |
+| 11 | 146 | near_trace | low-to-provisional | 2 | 5 | 9.08 | 1,424 |
+| 12 | 1255 | halo | low | 0 | 62 | 2.45 | 1,214 |
+| 13 | 2162 | near_trace | low-to-provisional | 2 | 157 | 10.03 | 1,077 |
+| 14 | 166 | near_trace | low | 0 | 97 | 1.73 | 974 |
+| 15 | 109 | halo | low-to-provisional | 1 | 10 | 4.37 | 908 |
+| 16 | 2076 | halo | low-to-provisional | 3 | 158 | 3.66 | 893 |
+| 17 | 678 | near_trace | low-to-provisional | 3 | 179 | 5.11 | 886 |
+| 18 | 1116 | near_trace | low | 0 | 173 | 2.50 | 878 |
+| 19 | 2140 | halo | low-to-provisional | 2 | 160 | 3.36 | 868 |
+| 20 | 2127 | halo | low-to-provisional | 1 | 160 | 4.26 | 865 |
+| 21 | 469 | halo | low-to-provisional | 1 | 52 | 14.23 | 857 |
+| 22 | 606 | near_trace | low-to-provisional | 1 | 161 | 2.39 | 813 |
+| 23 | 1661 | near_trace | low-to-provisional | 1 | 29 | 2.23 | 805 |
+| 24 | 319 | halo | low | 0 | 8 | 10.33 | 798 |
+| 25 | 2548 | near_trace | low | 0 | 23 | 3.49 | 731 |
+| 26 | 1317 | near_trace | low | 0 | 147 | 2.37 | 708 |
+| 27 | 953 | near_trace | low-to-provisional | 1 | 13 | 2.68 | 673 |
+| 28 | 1731 | halo | low-to-provisional | 3 | 5 | 9.39 | 669 |
+| 29 | 1970 | halo | low-to-provisional | 2 | 60 | 2.45 | 664 |
+| 30 | 1735 | halo | low-to-provisional | 2 | 8 | 12.40 | 654 |
+| 31 | 352 | near_trace | low-to-provisional | 1 | 6 | 8.62 | 637 |
+| 32 | 2347 | near_trace | low-to-provisional | 1 | 178 | 4.03 | 636 |
+| 33 | 1181 | near_trace | low-to-provisional | 1 | 22 | 4.87 | 628 |
+| 34 | 135 | near_trace | low-to-provisional | 1 | 174 | 4.42 | 618 |
+| 35 | 511 | near_trace | low-to-provisional | 2 | 1 | 8.05 | 617 |
+| 36 | 1373 | near_trace | low-to-provisional | 1 | 33 | 1.58 | 611 |
+| 37 | 262 | halo | low | 0 | 172 | 4.95 | 606 |
+| 38 | 1594 | halo | low | 0 | 8 | 9.24 | 589 |
+| 39 | 6 | near_trace | low | 0 | 152 | 4.10 | 587 |
+| 40 | 2106 | near_trace | low-to-provisional | 3 | 100 | 1.97 | 583 |
+| 41 | 1217 | halo | low | 0 | 167 | 2.51 | 578 |
+| 42 | 2268 | near_trace | low-to-provisional | 2 | 135 | 2.06 | 569 |
+| 43 | 1271 | near_trace | low | 0 | 26 | 15.93 | 544 |
+| 44 | 2302 | near_trace | low-to-provisional | 2 | 161 | 8.67 | 543 |
+| 45 | 1932 | near_trace | low-to-provisional | 1 | 20 | 4.12 | 538 |
+| 46 | 763 | halo | low-to-provisional | 1 | 171 | 7.58 | 507 |
+| 47 | 2517 | near_trace | low-to-provisional | 1 | 30 | 2.40 | 497 |
+| 48 | 1377 | halo | low | 0 | 59 | 1.51 | 495 |
+| 49 | 1563 | near_trace | low | 0 | 8 | 4.37 | 486 |
+| 50 | 274 | halo | low | 0 | 5 | 5.97 | 472 |
+| 51 | 564 | halo | low-to-provisional | 1 | 125 | 2.84 | 469 |
+| 52 | 1328 | halo | low | 0 | 20 | 5.49 | 463 |
+| 53 | 1404 | halo | low-to-provisional | 1 | 170 | 1.65 | 461 |
+| 54 | 2014 | near_trace | low-to-provisional | 1 | 175 | 2.86 | 456 |
+| 55 | 2424 | near_trace | low-to-provisional | 1 | 49 | 2.91 | 445 |
+| 56 | 213 | near_trace | low-to-provisional | 2 | 12 | 2.48 | 417 |
+| 57 | 510 | halo | low | 0 | 11 | 11.08 | 414 |
+| 58 | 2550 | near_trace | low-to-provisional | 1 | 30 | 2.60 | 413 |
+| 59 | 1743 | halo | low-to-provisional | 1 | 86 | 1.87 | 411 |
+| 60 | 2443 | halo | low | 0 | 10 | 6.26 | 403 |
+| 61 | 1952 | halo | low-to-provisional | 2 | 24 | 4.86 | 400 |
+| 62 | 334 | near_trace | low-to-provisional | 1 | 44 | 5.03 | 399 |
+| 63 | 371 | halo | low-to-provisional | 1 | 17 | 6.69 | 397 |
+| 64 | 1115 | near_trace | low | 0 | 51 | 10.39 | 397 |
+| 65 | 228 | near_trace | low-to-provisional | 1 | 16 | 5.64 | 394 |
+| 66 | 1110 | halo | low-to-provisional | 1 | 56 | 7.91 | 393 |
+| 67 | 2505 | halo | low | 0 | 17 | 7.34 | 390 |
+| 68 | 1539 | halo | low-to-provisional | 1 | 9 | 7.24 | 384 |
+| 69 | 1652 | halo | low-to-provisional | 1 | 16 | 3.63 | 379 |
+| 70 | 2561 | near_trace | low | 0 | 12 | 4.54 | 378 |
+| 71 | 2346 | halo | low | 0 | 19 | 3.52 | 368 |
+| 72 | 9 | halo | low-to-provisional | 1 | 149 | 5.16 | 364 |
+| 73 | 404 | near_trace | low-to-provisional | 1 | 172 | 4.54 | 360 |
+| 74 | 789 | near_trace | low-to-provisional | 2 | 10 | 1.90 | 356 |
+| 75 | 1862 | halo | low-to-provisional | 1 | 35 | 3.96 | 355 |
+| 76 | 726 | halo | low-to-provisional | 1 | 18 | 12.01 | 350 |
+| 77 | 1956 | halo | low-to-provisional | 2 | 157 | 8.06 | 345 |
+| 78 | 1938 | halo | low-to-provisional | 1 | 4 | 6.97 | 337 |
+| 79 | 1969 | halo | low-to-provisional | 1 | 17 | 5.88 | 336 |
+| 80 | 1907 | near_trace | low-to-provisional | 1 | 164 | 2.94 | 335 |
+| 81 | 1557 | near_trace | low-to-provisional | 1 | 13 | 2.78 | 332 |
+| 82 | 172 | halo | low | 0 | 1 | 4.38 | 331 |
+| 83 | 1571 | halo | low | 0 | 110 | 1.87 | 326 |
+| 84 | 1717 | near_trace | low-to-provisional | 2 | 133 | 3.99 | 323 |
+| 85 | 351 | halo | low | 0 | 176 | 2.99 | 322 |
+| 86 | 784 | near_trace | low | 0 | 26 | 4.83 | 314 |
+| 87 | 2381 | halo | low-to-provisional | 1 | 91 | 3.70 | 312 |
+| 88 | 1024 | halo | low-to-provisional | 1 | 162 | 6.96 | 309 |
+| 89 | 323 | halo | low-to-provisional | 1 | 11 | 10.39 | 304 |
+| 90 | 87 | near_trace | low | 0 | 20 | 3.30 | 303 |
+| 91 | 835 | near_trace | low | 0 | 171 | 5.49 | 294 |
+| 92 | 1111 | halo | low-to-provisional | 1 | 27 | 1.98 | 291 |
+| 93 | 1853 | halo | low-to-provisional | 2 | 179 | 5.10 | 289 |
+| 94 | 126 | halo | low-to-provisional | 1 | 49 | 7.90 | 275 |
+| 95 | 954 | halo | low-to-provisional | 1 | 23 | 9.05 | 274 |
+| 96 | 2524 | halo | low | 0 | 32 | 2.69 | 272 |
+| 97 | 2231 | near_trace | low-to-provisional | 2 | 15 | 1.87 | 263 |
+| 98 | 141 | halo | low | 0 | 3 | 1.97 | 263 |
+| 99 | 177 | halo | low-to-provisional | 1 | 28 | 3.56 | 257 |
+| 100 | 1039 | halo | low | 0 | 42 | 2.41 | 253 |
+| 101 | 906 | near_trace | low | 0 | 30 | 9.24 | 253 |
+| 102 | 419 | near_trace | low | 0 | 53 | 3.61 | 251 |
+| 103 | 1874 | near_trace | low-to-provisional | 2 | 28 | 2.38 | 246 |
+| 104 | 2581 | near_trace | low-to-provisional | 1 | 141 | 1.84 | 244 |
+| 105 | 1201 | halo | low | 0 | 1 | 5.05 | 244 |
+| 106 | 1635 | halo | low-to-provisional | 1 | 15 | 14.56 | 242 |
+| 107 | 1472 | near_trace | low | 0 | 140 | 2.73 | 240 |
+| 108 | 410 | halo | low-to-provisional | 1 | 172 | 4.84 | 240 |
+| 109 | 348 | halo | low | 0 | 50 | 1.89 | 237 |
+| 110 | 1854 | near_trace | low-to-provisional | 2 | 154 | 2.59 | 236 |
+| 111 | 1327 | near_trace | low-to-provisional | 1 | 155 | 2.27 | 234 |
+| 112 | 599 | halo | low | 0 | 24 | 8.13 | 232 |
+| 113 | 367 | halo | low-to-provisional | 2 | 12 | 6.27 | 229 |
+| 114 | 377 | halo | low-to-provisional | 1 | 43 | 6.50 | 227 |
+| 115 | 1400 | halo | low-to-provisional | 1 | 2 | 2.50 | 223 |
+| 116 | 258 | halo | low | 0 | 61 | 4.85 | 222 |
+| 117 | 669 | halo | low | 0 | 17 | 7.65 | 219 |
+| 118 | 744 | near_trace | low-to-provisional | 1 | 26 | 6.62 | 219 |
+| 119 | 1846 | halo | low-to-provisional | 3 | 87 | 1.24 | 218 |
+| 120 | 2139 | halo | low-to-provisional | 3 | 143 | 8.23 | 218 |
+| 121 | 1290 | near_trace | low | 0 | 53 | 4.04 | 217 |
+| 122 | 1574 | near_trace | low-to-provisional | 1 | 150 | 1.28 | 216 |
+| 123 | 10 | halo | low-to-provisional | 1 | 55 | 3.98 | 215 |
+| 124 | 1529 | near_trace | low-to-provisional | 1 | 141 | 2.49 | 209 |
+| 125 | 1170 | halo | low-to-provisional | 1 | 21 | 1.75 | 209 |
+| 126 | 730 | halo | low-to-provisional | 2 | 50 | 5.56 | 209 |
+| 127 | 273 | near_trace | low-to-provisional | 1 | 52 | 2.67 | 209 |
+| 128 | 958 | near_trace | low | 0 | 18 | 5.58 | 204 |
+| 129 | 667 | near_trace | low | 0 | 36 | 3.54 | 204 |
+| 130 | 2201 | halo | low-to-provisional | 2 | 132 | 1.86 | 198 |
+| 131 | 445 | near_trace | low-to-provisional | 1 | 20 | 5.07 | 197 |
+| 132 | 681 | near_trace | low | 0 | 25 | 1.21 | 194 |
+| 133 | 940 | near_trace | low | 0 | 12 | 1.83 | 188 |
+| 134 | 395 | halo | low-to-provisional | 1 | 15 | 8.65 | 186 |
+| 135 | 200 | near_trace | low-to-provisional | 1 | 40 | 8.26 | 184 |
+| 136 | 205 | halo | low | 0 | 29 | 8.76 | 184 |
+| 137 | 689 | halo | low | 0 | 174 | 9.90 | 182 |
+| 138 | 1089 | halo | low-to-provisional | 1 | 14 | 2.09 | 178 |
+| 139 | 223 | near_trace | low-to-provisional | 1 | 164 | 6.07 | 178 |
+| 140 | 139 | near_trace | low-to-provisional | 2 | 60 | 4.49 | 177 |
+| 141 | 1798 | near_trace | low-to-provisional | 2 | 24 | 8.05 | 177 |
+| 142 | 2130 | halo | low-to-provisional | 2 | 134 | 4.89 | 176 |
+| 143 | 2502 | halo | low | 0 | 6 | 4.61 | 176 |
+| 144 | 2533 | halo | low-to-provisional | 1 | 12 | 3.22 | 175 |
+| 145 | 1556 | halo | low-to-provisional | 1 | 99 | 1.37 | 172 |
+| 146 | 1284 | halo | low | 0 | 148 | 2.12 | 172 |
+| 147 | 618 | halo | low | 0 | 4 | 1.88 | 169 |
+| 148 | 1919 | halo | low-to-provisional | 2 | 44 | 1.88 | 166 |
+| 149 | 119 | near_trace | low | 0 | 72 | 1.43 | 165 |
+| 150 | 399 | near_trace | low-to-provisional | 2 | 3 | 4.85 | 164 |
+| 151 | 1349 | halo | low-to-provisional | 1 | 38 | 1.12 | 163 |
+| 152 | 1597 | halo | low-to-provisional | 1 | 178 | 4.24 | 161 |
+| 153 | 1699 | near_trace | low-to-provisional | 1 | 28 | 3.32 | 161 |
+| 154 | 1222 | near_trace | low-to-provisional | 1 | 126 | 4.26 | 159 |
+| 155 | 1094 | near_trace | low-to-provisional | 1 | 38 | 9.34 | 159 |
+| 156 | 1607 | halo | low | 0 | 25 | 3.46 | 157 |
+| 157 | 221 | halo | low-to-provisional | 1 | 9 | 4.61 | 154 |
+| 158 | 910 | near_trace | low | 0 | 50 | 5.18 | 154 |
+| 159 | 2488 | near_trace | low-to-provisional | 2 | 66 | 2.47 | 153 |
+| 160 | 5 | halo | low | 0 | 22 | 5.04 | 151 |
+| 161 | 2191 | near_trace | low | 0 | 45 | 1.67 | 147 |
+| 162 | 406 | near_trace | low-to-provisional | 1 | 25 | 2.44 | 146 |
+| 163 | 271 | halo | low-to-provisional | 1 | 0 | 8.66 | 145 |
+| 164 | 728 | isolated | low | 0 | 7 | 3.44 | 143 |
+| 165 | 316 | halo | low | 0 | 2 | 3.97 | 142 |
+| 166 | 167 | halo | low | 0 | 7 | 4.61 | 142 |
+| 167 | 878 | near_trace | low | 0 | 25 | 6.79 | 140 |
+| 168 | 532 | halo | low | 0 | 15 | 8.78 | 139 |
+| 169 | 463 | halo | low | 0 | 68 | 1.82 | 138 |
+| 170 | 282 | near_trace | low-to-provisional | 1 | 31 | 1.83 | 137 |
+| 171 | 1155 | near_trace | low-to-provisional | 1 | 0 | 2.90 | 136 |
+| 172 | 521 | near_trace | low-to-provisional | 1 | 26 | 5.75 | 133 |
+| 173 | 1341 | halo | low-to-provisional | 1 | 73 | 2.02 | 130 |
+| 174 | 1998 | halo | low-to-provisional | 1 | 164 | 4.06 | 129 |
+| 175 | 2016 | near_trace | low | 0 | 165 | 9.30 | 126 |
+| 176 | 1363 | near_trace | low | 0 | 33 | 2.40 | 120 |
+| 177 | 711 | halo | low | 0 | 32 | 1.86 | 119 |
+| 178 | 457 | halo | low | 0 | 3 | 1.59 | 108 |
+| 179 | 1484 | near_trace | low-to-provisional | 1 | 128 | 2.45 | 101 |
+| 180 | 2545 | halo | low | 0 | 16 | 4.53 | 98 |
+
+### 1. candidate 2353 (near_trace, confidence low-to-provisional)
+
+Closed group 2353: 11,580 emitted px (167.5 km2), PCA axes 29.1 x 8.6 km, elongation 3.36, centroid -118.469E 38.038N. Measured principal-axis azimuth 98deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: tmi (extremity, regional percentile 0.06); geod_2ndinv (high, regional percentile 1.00); geod_shearrate (high, regional percentile 0.99); geod_dilaterate (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (63% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 2. candidate 2460 (near_trace, confidence low-to-provisional)
+
+Closed group 2460: 8,914 emitted px (126.3 km2), PCA axes 70.3 x 7.2 km, elongation 9.80, centroid -118.068E 37.652N. Measured principal-axis azimuth 149deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.01); det_elev (extremity, regional percentile 0.05); geod_2ndinv (high, regional percentile 0.98); geod_shearrate (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 3. candidate 2217 (near_trace, confidence low-to-provisional)
+
+Closed group 2217: 5,397 emitted px (85.2 km2), PCA axes 18.6 x 10.4 km, elongation 1.77, centroid -117.856E 38.438N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 4. candidate 1293 (halo, confidence low-to-provisional)
+
+Closed group 1293: 4,596 emitted px (66.8 km2), PCA axes 34.5 x 6.6 km, elongation 5.24, centroid -118.165E 39.664N. Measured principal-axis azimuth 12deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.10); grav_asa (high, regional percentile 0.94); det_elev (extremity, regional percentile 0.09). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 5. candidate 1213 (near_trace, confidence low)
+
+Closed group 1213: 4,061 emitted px (47.1 km2), PCA axes 11.3 x 5.7 km, elongation 1.99, centroid -118.459E 39.806N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (60% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 6. candidate 292 (near_trace, confidence low-to-provisional)
+
+Closed group 292: 2,486 emitted px (31.5 km2), PCA axes 9.7 x 4.9 km, elongation 2.00, centroid -119.122E 40.421N. Measured principal-axis azimuth 13deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 7. candidate 2419 (near_trace, confidence low-to-provisional)
+
+Closed group 2419: 2,271 emitted px (35.0 km2), PCA axes 18.5 x 6.4 km, elongation 2.89, centroid -117.915E 37.864N. Measured principal-axis azimuth 23deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.92); geod_shearrate (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (73% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 8. candidate 2360 (near_trace, confidence low-to-provisional)
+
+Closed group 2360: 1,761 emitted px (25.1 km2), PCA axes 14.4 x 3.8 km, elongation 3.81, centroid -117.589E 38.020N. Measured principal-axis azimuth 46deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 9. candidate 1578 (near_trace, confidence low)
+
+Closed group 1578: 1,751 emitted px (27.9 km2), PCA axes 14.5 x 5.6 km, elongation 2.58, centroid -118.237E 39.404N. Measured principal-axis azimuth 79deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (70% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 10. candidate 795 (near_trace, confidence low-to-provisional)
+
+Closed group 795: 1,575 emitted px (23.4 km2), PCA axes 7.7 x 7.0 km, elongation 1.10, centroid -118.738E 40.121N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: det_elev (extremity, regional percentile 0.91); deq_n100a15 (low, regional percentile 0.08). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (56% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 11. candidate 146 (near_trace, confidence low-to-provisional)
+
+Closed group 146: 1,424 emitted px (23.5 km2), PCA axes 28.2 x 3.1 km, elongation 9.08, centroid -118.248E 40.503N. Measured principal-axis azimuth 5deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: grav_hgm (high, regional percentile 0.93); deq_n100a15 (low, regional percentile 0.06). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (70% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 12. candidate 1255 (halo, confidence low)
+
+Closed group 1255: 1,214 emitted px (15.1 km2), PCA axes 7.1 x 2.9 km, elongation 2.45, centroid -118.676E 39.786N. Measured principal-axis azimuth 62deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 13. candidate 2162 (near_trace, confidence low-to-provisional)
+
+Closed group 2162: 1,077 emitted px (14.2 km2), PCA axes 15.3 x 1.5 km, elongation 10.03, centroid -118.679E 38.524N. Measured principal-axis azimuth 157deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: tmi (extremity, regional percentile 0.02); rtp (extremity, regional percentile 0.01); det_elev (extremity, regional percentile 0.01); geod_2ndinv (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (68% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 14. candidate 166 (near_trace, confidence low)
+
+Closed group 166: 974 emitted px (16.6 km2), PCA axes 7.3 x 4.2 km, elongation 1.73, centroid -116.830E 40.581N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 15. candidate 109 (halo, confidence low-to-provisional)
+
+Closed group 109: 908 emitted px (12.2 km2), PCA axes 20.2 x 4.6 km, elongation 4.37, centroid -116.758E 40.596N. Measured principal-axis azimuth 10deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.96); grav_hgm (high, regional percentile 0.98); grav_asa (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 16. candidate 2076 (halo, confidence low-to-provisional)
+
+Closed group 2076: 893 emitted px (14.8 km2), PCA axes 10.3 x 2.8 km, elongation 3.66, centroid -118.111E 38.658N. Measured principal-axis azimuth 158deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 3/6 signal families reach the fault-favourable regional decile: magnetic_edge, seismicity, strain. Strongest measured support: mag_hgm (high, regional percentile 0.92); mag_asa (high, regional percentile 0.92); geod_dilaterate (high, regional percentile 0.94); ieq_n100a15 (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 17. candidate 678 (near_trace, confidence low-to-provisional)
+
+Closed group 678: 886 emitted px (12.5 km2), PCA axes 19.4 x 3.8 km, elongation 5.11, centroid -116.589E 40.154N. Measured principal-axis azimuth 179deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 3/6 signal families reach the fault-favourable regional decile: conductivity, gravity_edge, seismicity. Strongest measured support: grav_hgm (high, regional percentile 0.95); cond_surf (high, regional percentile 0.98); deq_n100a15 (low, regional percentile 0.02). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (74% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 18. candidate 1116 (near_trace, confidence low)
+
+Closed group 1116: 878 emitted px (13.8 km2), PCA axes 9.9 x 4.0 km, elongation 2.50, centroid -119.752E 39.898N. Measured principal-axis azimuth 173deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 19. candidate 2140 (halo, confidence low-to-provisional)
+
+Closed group 2140: 868 emitted px (12.1 km2), PCA axes 12.3 x 3.7 km, elongation 3.36, centroid -118.046E 38.559N. Measured principal-axis azimuth 160deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: det_elev (extremity, regional percentile 0.92); geod_dilaterate (high, regional percentile 0.93); ieq_n100a15 (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 20. candidate 2127 (halo, confidence low-to-provisional)
+
+Closed group 2127: 865 emitted px (13.3 km2), PCA axes 11.7 x 2.7 km, elongation 4.26, centroid -117.993E 38.601N. Measured principal-axis azimuth 160deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 21. candidate 469 (halo, confidence low-to-provisional)
+
+Closed group 469: 857 emitted px (10.4 km2), PCA axes 23.6 x 1.7 km, elongation 14.23, centroid -116.463E 40.297N. Measured principal-axis azimuth 52deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.93); grav_hgm (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 22. candidate 606 (near_trace, confidence low-to-provisional)
+
+Closed group 606: 813 emitted px (13.1 km2), PCA axes 8.5 x 3.5 km, elongation 2.39, centroid -118.749E 40.203N. Measured principal-axis azimuth 161deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: det_elev (extremity, regional percentile 0.92); deq_n100a15 (low, regional percentile 0.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (61% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 23. candidate 1661 (near_trace, confidence low-to-provisional)
+
+Closed group 1661: 805 emitted px (12.6 km2), PCA axes 7.4 x 3.3 km, elongation 2.23, centroid -118.184E 39.344N. Measured principal-axis azimuth 29deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: det_elev (extremity, regional percentile 0.09); geod_shearrate (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (73% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 24. candidate 319 (halo, confidence low)
+
+Closed group 319: 798 emitted px (10.7 km2), PCA axes 17.4 x 1.7 km, elongation 10.33, centroid -117.468E 40.394N. Measured principal-axis azimuth 8deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 25. candidate 2548 (near_trace, confidence low)
+
+Closed group 2548: 731 emitted px (12.9 km2), PCA axes 10.9 x 3.1 km, elongation 3.49, centroid -117.450E 37.638N. Measured principal-axis azimuth 23deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (65% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 26. candidate 1317 (near_trace, confidence low)
+
+Closed group 1317: 708 emitted px (11.3 km2), PCA axes 7.0 x 3.0 km, elongation 2.37, centroid -119.338E 39.721N. Measured principal-axis azimuth 147deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (74% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 27. candidate 953 (near_trace, confidence low-to-provisional)
+
+Closed group 953: 673 emitted px (10.7 km2), PCA axes 6.5 x 2.4 km, elongation 2.68, centroid -118.811E 40.054N. Measured principal-axis azimuth 13deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.92); rtp (extremity, regional percentile 0.93); grav_hgm (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (65% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 28. candidate 1731 (halo, confidence low-to-provisional)
+
+Closed group 1731: 669 emitted px (9.5 km2), PCA axes 18.9 x 2.0 km, elongation 9.39, centroid -118.328E 39.239N. Measured principal-axis azimuth 5deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 3/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity, strain. Strongest measured support: grav_asa (high, regional percentile 0.97); geod_shearrate (high, regional percentile 0.90); ieq_n100a15 (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 29. candidate 1970 (halo, confidence low-to-provisional)
+
+Closed group 1970: 664 emitted px (10.4 km2), PCA axes 6.3 x 2.6 km, elongation 2.45, centroid -118.245E 38.876N. Measured principal-axis azimuth 60deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: grav_hgm (high, regional percentile 0.99); grav_asa (high, regional percentile 0.98); det_elev (extremity, regional percentile 0.06); ieq_n100a15 (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 30. candidate 1735 (halo, confidence low-to-provisional)
+
+Closed group 1735: 654 emitted px (9.6 km2), PCA axes 18.4 x 1.5 km, elongation 12.40, centroid -118.141E 39.227N. Measured principal-axis azimuth 8deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.97); geod_shearrate (high, regional percentile 0.97); ieq_n100a15 (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 31. candidate 352 (near_trace, confidence low-to-provisional)
+
+Closed group 352: 637 emitted px (8.9 km2), PCA axes 11.4 x 1.3 km, elongation 8.62, centroid -119.301E 40.377N. Measured principal-axis azimuth 6deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (67% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 32. candidate 2347 (near_trace, confidence low-to-provisional)
+
+Closed group 2347: 636 emitted px (10.0 km2), PCA axes 8.4 x 2.1 km, elongation 4.03, centroid -117.885E 38.105N. Measured principal-axis azimuth 178deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (67% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 33. candidate 1181 (near_trace, confidence low-to-provisional)
+
+Closed group 1181: 628 emitted px (8.9 km2), PCA axes 13.1 x 2.7 km, elongation 4.87, centroid -118.031E 39.848N. Measured principal-axis azimuth 22deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.99); det_elev (extremity, regional percentile 0.08). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (74% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 34. candidate 135 (near_trace, confidence low-to-provisional)
+
+Closed group 135: 618 emitted px (9.3 km2), PCA axes 8.3 x 1.9 km, elongation 4.42, centroid -117.622E 40.609N. Measured principal-axis azimuth 174deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 4.0 px (44% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 35. candidate 511 (near_trace, confidence low-to-provisional)
+
+Closed group 511: 617 emitted px (8.1 km2), PCA axes 11.0 x 1.4 km, elongation 8.05, centroid -119.016E 40.267N. Measured principal-axis azimuth 1deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: tmi (extremity, regional percentile 0.07); grav_asa (high, regional percentile 0.98); deq_n100a15 (low, regional percentile 0.01). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (65% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 36. candidate 1373 (near_trace, confidence low-to-provisional)
+
+Closed group 1373: 611 emitted px (11.6 km2), PCA axes 7.0 x 4.4 km, elongation 1.58, centroid -119.044E 39.646N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: mag_hgm (high, regional percentile 0.90); mag_asa (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (56% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 37. candidate 262 (halo, confidence low)
+
+Closed group 262: 606 emitted px (9.1 km2), PCA axes 13.1 x 2.6 km, elongation 4.95, centroid -117.534E 40.466N. Measured principal-axis azimuth 172deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 38. candidate 1594 (halo, confidence low)
+
+Closed group 1594: 589 emitted px (9.1 km2), PCA axes 14.7 x 1.6 km, elongation 9.24, centroid -118.071E 39.362N. Measured principal-axis azimuth 8deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 39. candidate 6 (near_trace, confidence low)
+
+Closed group 6: 587 emitted px (8.0 km2), PCA axes 8.3 x 2.0 km, elongation 4.10, centroid -117.653E 40.696N. Measured principal-axis azimuth 152deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.8 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 40. candidate 2106 (near_trace, confidence low-to-provisional)
+
+Closed group 2106: 583 emitted px (11.6 km2), PCA axes 7.8 x 4.0 km, elongation 1.97, centroid -118.468E 38.637N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 3/6 signal families reach the fault-favourable regional decile: conductivity, seismicity, strain. Strongest measured support: rtp (extremity, regional percentile 0.04); geod_dilaterate (high, regional percentile 0.98); cond_surf (high, regional percentile 0.97); ieq_n100a15 (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (71% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 41. candidate 1217 (halo, confidence low)
+
+Closed group 1217: 578 emitted px (8.6 km2), PCA axes 6.6 x 2.6 km, elongation 2.51, centroid -119.397E 39.809N. Measured principal-axis azimuth 167deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 42. candidate 2268 (near_trace, confidence low-to-provisional)
+
+Closed group 2268: 569 emitted px (10.8 km2), PCA axes 6.2 x 3.0 km, elongation 2.06, centroid -118.623E 38.439N. Measured principal-axis azimuth 135deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: tmi (extremity, regional percentile 0.02); geod_2ndinv (high, regional percentile 0.99); geod_dilaterate (high, regional percentile 1.00); ieq_n100a15 (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 43. candidate 1271 (near_trace, confidence low)
+
+Closed group 1271: 544 emitted px (6.7 km2), PCA axes 15.9 x 1.0 km, elongation 15.93, centroid -118.257E 39.756N. Measured principal-axis azimuth 26deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 5.4 px (22% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 44. candidate 2302 (near_trace, confidence low-to-provisional)
+
+Closed group 2302: 543 emitted px (8.2 km2), PCA axes 13.0 x 1.5 km, elongation 8.67, centroid -118.062E 38.383N. Measured principal-axis azimuth 161deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: grav_hgm (high, regional percentile 0.97); grav_asa (high, regional percentile 0.92); det_elev (extremity, regional percentile 0.10); ieq_n100a15 (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (59% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 45. candidate 1932 (near_trace, confidence low-to-provisional)
+
+Closed group 1932: 538 emitted px (8.4 km2), PCA axes 7.5 x 1.8 km, elongation 4.12, centroid -118.091E 38.995N. Measured principal-axis azimuth 20deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (56% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 46. candidate 763 (halo, confidence low-to-provisional)
+
+Closed group 763: 507 emitted px (7.1 km2), PCA axes 10.6 x 1.4 km, elongation 7.58, centroid -119.049E 40.117N. Measured principal-axis azimuth 171deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.09); grav_asa (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 47. candidate 2517 (near_trace, confidence low-to-provisional)
+
+Closed group 2517: 497 emitted px (9.8 km2), PCA axes 6.2 x 2.6 km, elongation 2.40, centroid -117.415E 37.699N. Measured principal-axis azimuth 30deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 48. candidate 1377 (halo, confidence low)
+
+Closed group 1377: 495 emitted px (7.3 km2), PCA axes 4.4 x 2.9 km, elongation 1.51, centroid -119.278E 39.641N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 49. candidate 1563 (near_trace, confidence low)
+
+Closed group 1563: 486 emitted px (7.2 km2), PCA axes 11.3 x 2.6 km, elongation 4.37, centroid -118.105E 39.416N. Measured principal-axis azimuth 8deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (63% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 50. candidate 274 (halo, confidence low)
+
+Closed group 274: 472 emitted px (6.9 km2), PCA axes 7.7 x 1.3 km, elongation 5.97, centroid -117.435E 40.465N. Measured principal-axis azimuth 5deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 51. candidate 564 (halo, confidence low-to-provisional)
+
+Closed group 564: 469 emitted px (8.8 km2), PCA axes 7.0 x 2.5 km, elongation 2.84, centroid -119.698E 40.230N. Measured principal-axis azimuth 125deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.10); mag_hgm (high, regional percentile 0.96); mag_asa (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 52. candidate 1328 (halo, confidence low)
+
+Closed group 1328: 463 emitted px (6.9 km2), PCA axes 8.9 x 1.6 km, elongation 5.49, centroid -119.149E 39.703N. Measured principal-axis azimuth 20deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 53. candidate 1404 (halo, confidence low-to-provisional)
+
+Closed group 1404: 461 emitted px (7.6 km2), PCA axes 4.6 x 2.8 km, elongation 1.65, centroid -119.633E 39.593N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: mag_hgm (high, regional percentile 0.94); mag_asa (high, regional percentile 0.94); rtp (extremity, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 54. candidate 2014 (near_trace, confidence low-to-provisional)
+
+Closed group 2014: 456 emitted px (7.0 km2), PCA axes 6.1 x 2.1 km, elongation 2.86, centroid -118.018E 38.794N. Measured principal-axis azimuth 175deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (73% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 55. candidate 2424 (near_trace, confidence low-to-provisional)
+
+Closed group 2424: 445 emitted px (7.4 km2), PCA axes 7.1 x 2.4 km, elongation 2.91, centroid -118.369E 37.914N. Measured principal-axis azimuth 49deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.08); geod_2ndinv (high, regional percentile 0.99); geod_shearrate (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (70% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 56. candidate 213 (near_trace, confidence low-to-provisional)
+
+Closed group 213: 417 emitted px (6.4 km2), PCA axes 6.0 x 2.4 km, elongation 2.48, centroid -118.106E 40.531N. Measured principal-axis azimuth 12deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: iso_grav_anom (extremity, regional percentile 1.00); grav_asa (high, regional percentile 0.96); deq_n100a15 (low, regional percentile 0.08). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.0 px (52% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 57. candidate 510 (halo, confidence low)
+
+Closed group 510: 414 emitted px (5.6 km2), PCA axes 13.5 x 1.2 km, elongation 11.08, centroid -117.636E 40.273N. Measured principal-axis azimuth 11deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 58. candidate 2550 (near_trace, confidence low-to-provisional)
+
+Closed group 2550: 413 emitted px (6.7 km2), PCA axes 6.0 x 2.3 km, elongation 2.60, centroid -117.594E 37.654N. Measured principal-axis azimuth 30deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.97); grav_asa (high, regional percentile 0.94); det_elev (extremity, regional percentile 0.03); depth_to_base_surf (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (61% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 59. candidate 1743 (halo, confidence low-to-provisional)
+
+Closed group 1743: 411 emitted px (5.7 km2), PCA axes 4.0 x 2.1 km, elongation 1.87, centroid -118.492E 39.274N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.97); geod_shearrate (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 60. candidate 2443 (halo, confidence low)
+
+Closed group 2443: 403 emitted px (6.0 km2), PCA axes 12.2 x 1.9 km, elongation 6.26, centroid -117.472E 37.847N. Measured principal-axis azimuth 10deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 61. candidate 1952 (halo, confidence low-to-provisional)
+
+Closed group 1952: 400 emitted px (5.8 km2), PCA axes 6.4 x 1.3 km, elongation 4.86, centroid -118.105E 38.924N. Measured principal-axis azimuth 24deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, strain. Strongest measured support: grav_asa (high, regional percentile 0.97); geod_dilaterate (high, regional percentile 0.92); depth_to_base_surf (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 62. candidate 334 (near_trace, confidence low-to-provisional)
+
+Closed group 334: 399 emitted px (4.9 km2), PCA axes 10.1 x 2.0 km, elongation 5.03, centroid -116.924E 40.435N. Measured principal-axis azimuth 44deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (67% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 63. candidate 371 (halo, confidence low-to-provisional)
+
+Closed group 371: 397 emitted px (5.1 km2), PCA axes 8.6 x 1.3 km, elongation 6.69, centroid -117.589E 40.385N. Measured principal-axis azimuth 17deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 64. candidate 1115 (near_trace, confidence low)
+
+Closed group 1115: 397 emitted px (4.9 km2), PCA axes 12.1 x 1.2 km, elongation 10.39, centroid -118.638E 39.915N. Measured principal-axis azimuth 51deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.8 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 65. candidate 228 (near_trace, confidence low-to-provisional)
+
+Closed group 228: 394 emitted px (5.6 km2), PCA axes 8.1 x 1.4 km, elongation 5.64, centroid -118.969E 40.495N. Measured principal-axis azimuth 16deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 1.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (60% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 66. candidate 1110 (halo, confidence low-to-provisional)
+
+Closed group 1110: 393 emitted px (5.3 km2), PCA axes 16.1 x 2.0 km, elongation 7.91, centroid -117.946E 39.938N. Measured principal-axis azimuth 56deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.92); grav_asa (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 67. candidate 2505 (halo, confidence low)
+
+Closed group 2505: 390 emitted px (5.9 km2), PCA axes 14.0 x 1.9 km, elongation 7.34, centroid -117.498E 37.699N. Measured principal-axis azimuth 17deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 68. candidate 1539 (halo, confidence low-to-provisional)
+
+Closed group 1539: 384 emitted px (6.3 km2), PCA axes 10.9 x 1.5 km, elongation 7.24, centroid -118.531E 39.442N. Measured principal-axis azimuth 9deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: tmi (extremity, regional percentile 0.90); cond_surf (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 69. candidate 1652 (halo, confidence low-to-provisional)
+
+Closed group 1652: 379 emitted px (5.7 km2), PCA axes 9.2 x 2.5 km, elongation 3.63, centroid -118.570E 39.346N. Measured principal-axis azimuth 16deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 70. candidate 2561 (near_trace, confidence low)
+
+Closed group 2561: 378 emitted px (6.1 km2), PCA axes 7.2 x 1.6 km, elongation 4.54, centroid -117.519E 37.614N. Measured principal-axis azimuth 12deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 3.1 px (50% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 71. candidate 2346 (halo, confidence low)
+
+Closed group 2346: 368 emitted px (5.9 km2), PCA axes 6.5 x 1.8 km, elongation 3.52, centroid -117.478E 38.115N. Measured principal-axis azimuth 19deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 72. candidate 9 (halo, confidence low-to-provisional)
+
+Closed group 9: 364 emitted px (5.3 km2), PCA axes 6.2 x 1.2 km, elongation 5.16, centroid -116.872E 40.706N. Measured principal-axis azimuth 149deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.92); grav_hgm (high, regional percentile 0.99); grav_asa (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 73. candidate 404 (near_trace, confidence low-to-provisional)
+
+Closed group 404: 360 emitted px (5.1 km2), PCA axes 6.8 x 1.5 km, elongation 4.54, centroid -118.244E 40.360N. Measured principal-axis azimuth 172deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: deq_n100a15 (low, regional percentile 0.05). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (58% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 74. candidate 789 (near_trace, confidence low-to-provisional)
+
+Closed group 789: 356 emitted px (5.6 km2), PCA axes 3.8 x 2.0 km, elongation 1.90, centroid -116.162E 40.150N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: grav_hgm (high, regional percentile 0.99); grav_asa (high, regional percentile 0.97); depth_to_base_surf (high, regional percentile 0.99); deq_n100a15 (low, regional percentile 0.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (70% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 75. candidate 1862 (halo, confidence low-to-provisional)
+
+Closed group 1862: 355 emitted px (5.6 km2), PCA axes 7.5 x 1.9 km, elongation 3.96, centroid -118.175E 39.119N. Measured principal-axis azimuth 35deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: ieq_n100a15 (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 76. candidate 726 (halo, confidence low-to-provisional)
+
+Closed group 726: 350 emitted px (4.8 km2), PCA axes 10.9 x 0.9 km, elongation 12.01, centroid -117.845E 40.153N. Measured principal-axis azimuth 18deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 77. candidate 1956 (halo, confidence low-to-provisional)
+
+Closed group 1956: 345 emitted px (5.0 km2), PCA axes 11.8 x 1.5 km, elongation 8.06, centroid -118.812E 38.899N. Measured principal-axis azimuth 157deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, strain. Strongest measured support: grav_asa (high, regional percentile 0.91); geod_2ndinv (high, regional percentile 0.94); geod_shearrate (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 78. candidate 1938 (halo, confidence low-to-provisional)
+
+Closed group 1938: 337 emitted px (4.4 km2), PCA axes 7.8 x 1.1 km, elongation 6.97, centroid -118.161E 38.976N. Measured principal-axis azimuth 4deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 79. candidate 1969 (halo, confidence low-to-provisional)
+
+Closed group 1969: 336 emitted px (5.8 km2), PCA axes 9.0 x 1.5 km, elongation 5.88, centroid -117.914E 38.872N. Measured principal-axis azimuth 17deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: det_elev (extremity, regional percentile 0.06); cond_surf (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 80. candidate 1907 (near_trace, confidence low-to-provisional)
+
+Closed group 1907: 335 emitted px (6.8 km2), PCA axes 8.3 x 2.8 km, elongation 2.94, centroid -118.894E 39.028N. Measured principal-axis azimuth 164deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: rtp (extremity, regional percentile 0.09); geod_2ndinv (high, regional percentile 0.96); geod_shearrate (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (75% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 81. candidate 1557 (near_trace, confidence low-to-provisional)
+
+Closed group 1557: 332 emitted px (4.4 km2), PCA axes 4.3 x 1.6 km, elongation 2.78, centroid -118.216E 39.444N. Measured principal-axis azimuth 13deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 5.0 px (33% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 82. candidate 172 (halo, confidence low)
+
+Closed group 172: 331 emitted px (4.9 km2), PCA axes 6.2 x 1.4 km, elongation 4.38, centroid -117.489E 40.574N. Measured principal-axis azimuth 1deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 83. candidate 1571 (halo, confidence low)
+
+Closed group 1571: 326 emitted px (7.0 km2), PCA axes 5.1 x 2.7 km, elongation 1.87, centroid -119.301E 39.422N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 84. candidate 1717 (near_trace, confidence low-to-provisional)
+
+Closed group 1717: 323 emitted px (4.3 km2), PCA axes 5.0 x 1.3 km, elongation 3.99, centroid -118.868E 39.305N. Measured principal-axis azimuth 133deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, magnetic_edge. Strongest measured support: mag_hgm (high, regional percentile 0.93); mag_asa (high, regional percentile 0.93); rtp (extremity, regional percentile 0.98); grav_asa (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (71% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 85. candidate 351 (halo, confidence low)
+
+Closed group 351: 322 emitted px (6.1 km2), PCA axes 5.8 x 1.9 km, elongation 2.99, centroid -119.391E 40.395N. Measured principal-axis azimuth 176deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 86. candidate 784 (near_trace, confidence low)
+
+Closed group 784: 314 emitted px (4.3 km2), PCA axes 8.6 x 1.8 km, elongation 4.83, centroid -117.063E 40.136N. Measured principal-axis azimuth 26deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (58% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 87. candidate 2381 (halo, confidence low-to-provisional)
+
+Closed group 2381: 312 emitted px (4.9 km2), PCA axes 5.7 x 1.6 km, elongation 3.70, centroid -118.092E 38.005N. Measured principal-axis azimuth 91deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.97); geod_shearrate (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 88. candidate 1024 (halo, confidence low-to-provisional)
+
+Closed group 1024: 309 emitted px (4.2 km2), PCA axes 10.0 x 1.4 km, elongation 6.96, centroid -119.290E 40.008N. Measured principal-axis azimuth 162deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.09); grav_hgm (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 89. candidate 323 (halo, confidence low-to-provisional)
+
+Closed group 323: 304 emitted px (4.0 km2), PCA axes 9.5 x 0.9 km, elongation 10.39, centroid -116.273E 40.422N. Measured principal-axis azimuth 11deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: deq_n100a15 (low, regional percentile 0.06). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.7 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 90. candidate 87 (near_trace, confidence low)
+
+Closed group 87: 303 emitted px (4.6 km2), PCA axes 6.5 x 2.0 km, elongation 3.30, centroid -119.141E 40.626N. Measured principal-axis azimuth 20deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 91. candidate 835 (near_trace, confidence low)
+
+Closed group 835: 294 emitted px (4.3 km2), PCA axes 7.2 x 1.3 km, elongation 5.49, centroid -119.294E 40.091N. Measured principal-axis azimuth 171deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (70% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 92. candidate 1111 (halo, confidence low-to-provisional)
+
+Closed group 1111: 291 emitted px (4.8 km2), PCA axes 3.7 x 1.9 km, elongation 1.98, centroid -118.998E 39.937N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 93. candidate 1853 (halo, confidence low-to-provisional)
+
+Closed group 1853: 289 emitted px (4.1 km2), PCA axes 9.0 x 1.8 km, elongation 5.10, centroid -118.321E 39.120N. Measured principal-axis azimuth 179deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.92); ieq_n100a15 (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 94. candidate 126 (halo, confidence low-to-provisional)
+
+Closed group 126: 275 emitted px (3.4 km2), PCA axes 6.8 x 0.9 km, elongation 7.90, centroid -117.935E 40.627N. Measured principal-axis azimuth 49deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: deq_n100a15 (low, regional percentile 0.04). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 95. candidate 954 (halo, confidence low-to-provisional)
+
+Closed group 954: 274 emitted px (4.4 km2), PCA axes 8.3 x 0.9 km, elongation 9.05, centroid -117.811E 40.053N. Measured principal-axis azimuth 23deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 96. candidate 2524 (halo, confidence low)
+
+Closed group 2524: 272 emitted px (5.0 km2), PCA axes 5.9 x 2.2 km, elongation 2.69, centroid -117.559E 37.696N. Measured principal-axis azimuth 32deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 97. candidate 2231 (near_trace, confidence low-to-provisional)
+
+Closed group 2231: 263 emitted px (5.4 km2), PCA axes 5.3 x 2.8 km, elongation 1.87, centroid -118.372E 38.468N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.98); geod_dilaterate (high, regional percentile 0.97); ieq_n100a15 (high, regional percentile 1.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.0 px (54% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 98. candidate 141 (halo, confidence low)
+
+Closed group 141: 263 emitted px (4.7 km2), PCA axes 5.0 x 2.5 km, elongation 1.97, centroid -117.242E 40.609N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 99. candidate 177 (halo, confidence low-to-provisional)
+
+Closed group 177: 257 emitted px (3.9 km2), PCA axes 4.7 x 1.3 km, elongation 3.56, centroid -117.970E 40.575N. Measured principal-axis azimuth 28deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.93); deq_n100a15 (low, regional percentile 0.09). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 100. candidate 1039 (halo, confidence low)
+
+Closed group 1039: 253 emitted px (4.5 km2), PCA axes 4.0 x 1.7 km, elongation 2.41, centroid -117.623E 40.045N. Measured principal-axis azimuth 42deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 101. candidate 906 (near_trace, confidence low)
+
+Closed group 906: 253 emitted px (3.0 km2), PCA axes 7.3 x 0.8 km, elongation 9.24, centroid -117.545E 40.086N. Measured principal-axis azimuth 30deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (64% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 102. candidate 419 (near_trace, confidence low)
+
+Closed group 419: 251 emitted px (3.2 km2), PCA axes 4.8 x 1.3 km, elongation 3.61, centroid -116.347E 40.370N. Measured principal-axis azimuth 53deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (66% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 103. candidate 1874 (near_trace, confidence low-to-provisional)
+
+Closed group 1874: 246 emitted px (4.5 km2), PCA axes 5.9 x 2.5 km, elongation 2.38, centroid -118.410E 39.097N. Measured principal-axis azimuth 28deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: rtp (extremity, regional percentile 0.03); geod_dilaterate (high, regional percentile 0.95); ieq_n100a15 (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.0 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 104. candidate 2581 (near_trace, confidence low-to-provisional)
+
+Closed group 2581: 244 emitted px (4.3 km2), PCA axes 3.9 x 2.1 km, elongation 1.84, centroid -117.813E 37.420N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: tmi (extremity, regional percentile 0.04); iso_grav_anom (extremity, regional percentile 0.01); geod_shearrate (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (62% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 105. candidate 1201 (halo, confidence low)
+
+Closed group 1201: 244 emitted px (3.5 km2), PCA axes 7.0 x 1.4 km, elongation 5.05, centroid -118.217E 39.837N. Measured principal-axis azimuth 1deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 106. candidate 1635 (halo, confidence low-to-provisional)
+
+Closed group 1635: 242 emitted px (3.5 km2), PCA axes 11.8 x 0.8 km, elongation 14.56, centroid -117.976E 39.364N. Measured principal-axis azimuth 15deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.05); geod_dilaterate (high, regional percentile 0.94); depth_to_base_surf (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: bodies of this width are more consistent with a broad structural corridor (damage zone, basin-fill thickness change, or alteration halo) than with a single discrete fault trace; the mask may be enclosing a zone rather than a plane.
+
+### 107. candidate 1472 (near_trace, confidence low)
+
+Closed group 1472: 240 emitted px (3.5 km2), PCA axes 4.5 x 1.6 km, elongation 2.73, centroid -117.994E 39.542N. Measured principal-axis azimuth 140deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.9 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 108. candidate 410 (halo, confidence low-to-provisional)
+
+Closed group 410: 240 emitted px (3.5 km2), PCA axes 5.8 x 1.2 km, elongation 4.84, centroid -119.641E 40.336N. Measured principal-axis azimuth 172deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.93); geod_dilaterate (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 109. candidate 348 (halo, confidence low)
+
+Closed group 348: 237 emitted px (4.1 km2), PCA axes 3.5 x 1.8 km, elongation 1.89, centroid -118.687E 40.425N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 110. candidate 1854 (near_trace, confidence low-to-provisional)
+
+Closed group 1854: 236 emitted px (4.2 km2), PCA axes 5.6 x 2.2 km, elongation 2.59, centroid -118.232E 39.128N. Measured principal-axis azimuth 154deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.93); ieq_n100a15 (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (61% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 111. candidate 1327 (near_trace, confidence low-to-provisional)
+
+Closed group 1327: 234 emitted px (3.9 km2), PCA axes 5.5 x 2.4 km, elongation 2.27, centroid -119.582E 39.703N. Measured principal-axis azimuth 155deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: tmi (extremity, regional percentile 0.05); iso_grav_anom (extremity, regional percentile 0.08); cond_surf (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 112. candidate 599 (halo, confidence low)
+
+Closed group 599: 232 emitted px (3.0 km2), PCA axes 7.9 x 1.0 km, elongation 8.13, centroid -118.161E 40.227N. Measured principal-axis azimuth 24deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 113. candidate 367 (halo, confidence low-to-provisional)
+
+Closed group 367: 229 emitted px (3.3 km2), PCA axes 7.9 x 1.2 km, elongation 6.27, centroid -118.823E 40.377N. Measured principal-axis azimuth 12deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: conductivity, seismicity. Strongest measured support: rtp (extremity, regional percentile 0.09); cond_surf (high, regional percentile 0.97); deq_n100a15 (low, regional percentile 0.09). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 114. candidate 377 (halo, confidence low-to-provisional)
+
+Closed group 377: 227 emitted px (3.3 km2), PCA axes 7.1 x 1.1 km, elongation 6.50, centroid -119.044E 40.378N. Measured principal-axis azimuth 43deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 1.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 115. candidate 1400 (halo, confidence low-to-provisional)
+
+Closed group 1400: 223 emitted px (4.3 km2), PCA axes 5.3 x 2.1 km, elongation 2.50, centroid -119.666E 39.596N. Measured principal-axis azimuth 2deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.01); mag_hgm (high, regional percentile 0.96); mag_asa (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 116. candidate 258 (halo, confidence low)
+
+Closed group 258: 222 emitted px (3.0 km2), PCA axes 5.1 x 1.1 km, elongation 4.85, centroid -116.785E 40.503N. Measured principal-axis azimuth 61deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 117. candidate 669 (halo, confidence low)
+
+Closed group 669: 219 emitted px (3.0 km2), PCA axes 7.1 x 0.9 km, elongation 7.65, centroid -117.465E 40.201N. Measured principal-axis azimuth 17deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 118. candidate 744 (near_trace, confidence low-to-provisional)
+
+Closed group 744: 219 emitted px (2.4 km2), PCA axes 4.8 x 0.7 km, elongation 6.62, centroid -119.210E 40.153N. Measured principal-axis azimuth 26deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: det_elev (extremity, regional percentile 0.92); cond_surf (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.0 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 119. candidate 1846 (halo, confidence low-to-provisional)
+
+Closed group 1846: 218 emitted px (3.5 km2), PCA axes 4.5 x 3.6 km, elongation 1.24, centroid -118.083E 39.141N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 3/6 signal families reach the fault-favourable regional decile: magnetic_edge, seismicity, strain. Strongest measured support: mag_hgm (high, regional percentile 0.98); mag_asa (high, regional percentile 0.98); geod_2ndinv (high, regional percentile 0.93); geod_shearrate (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 120. candidate 2139 (halo, confidence low-to-provisional)
+
+Closed group 2139: 218 emitted px (3.4 km2), PCA axes 8.6 x 1.1 km, elongation 8.23, centroid -118.331E 38.596N. Measured principal-axis azimuth 143deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 3/6 signal families reach the fault-favourable regional decile: magnetic_edge, seismicity, strain. Strongest measured support: tmi (extremity, regional percentile 0.98); mag_hgm (high, regional percentile 0.91); mag_asa (high, regional percentile 0.91); geod_dilaterate (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 121. candidate 1290 (near_trace, confidence low)
+
+Closed group 1290: 217 emitted px (4.8 km2), PCA axes 8.0 x 2.0 km, elongation 4.04, centroid -118.873E 39.764N. Measured principal-axis azimuth 53deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (67% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 122. candidate 1574 (near_trace, confidence low-to-provisional)
+
+Closed group 1574: 216 emitted px (3.7 km2), PCA axes 2.6 x 2.0 km, elongation 1.28, centroid -119.261E 39.424N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.91); grav_hgm (high, regional percentile 1.00); grav_asa (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 123. candidate 10 (halo, confidence low-to-provisional)
+
+Closed group 10: 215 emitted px (3.2 km2), PCA axes 6.6 x 1.7 km, elongation 3.98, centroid -116.751E 40.709N. Measured principal-axis azimuth 55deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: tmi (extremity, regional percentile 0.97); rtp (extremity, regional percentile 0.94); iso_grav_anom (extremity, regional percentile 0.98); grav_asa (high, regional percentile 0.99). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 124. candidate 1529 (near_trace, confidence low-to-provisional)
+
+Closed group 1529: 209 emitted px (3.0 km2), PCA axes 4.4 x 1.8 km, elongation 2.49, centroid -119.165E 39.468N. Measured principal-axis azimuth 141deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.91); mag_hgm (high, regional percentile 0.98); mag_asa (high, regional percentile 0.98); rtp (extremity, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (57% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 125. candidate 1170 (halo, confidence low-to-provisional)
+
+Closed group 1170: 209 emitted px (2.9 km2), PCA axes 2.6 x 1.5 km, elongation 1.75, centroid -119.154E 39.883N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.00); mag_hgm (high, regional percentile 0.99); mag_asa (high, regional percentile 0.99); rtp (extremity, regional percentile 0.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 126. candidate 730 (halo, confidence low-to-provisional)
+
+Closed group 730: 209 emitted px (2.8 km2), PCA axes 9.4 x 1.7 km, elongation 5.56, centroid -116.653E 40.176N. Measured principal-axis azimuth 50deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: conductivity, seismicity. Strongest measured support: cond_surf (high, regional percentile 0.98); deq_n100a15 (low, regional percentile 0.01). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 127. candidate 273 (near_trace, confidence low-to-provisional)
+
+Closed group 273: 209 emitted px (2.8 km2), PCA axes 4.0 x 1.5 km, elongation 2.67, centroid -116.844E 40.490N. Measured principal-axis azimuth 52deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (74% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 128. candidate 958 (near_trace, confidence low)
+
+Closed group 958: 204 emitted px (3.1 km2), PCA axes 6.2 x 1.1 km, elongation 5.58, centroid -117.011E 40.067N. Measured principal-axis azimuth 18deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 3.2 px (48% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 129. candidate 667 (near_trace, confidence low)
+
+Closed group 667: 204 emitted px (2.8 km2), PCA axes 4.5 x 1.3 km, elongation 3.54, centroid -118.379E 40.204N. Measured principal-axis azimuth 36deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.2 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 130. candidate 2201 (halo, confidence low-to-provisional)
+
+Closed group 2201: 198 emitted px (3.5 km2), PCA axes 3.5 x 1.9 km, elongation 1.86, centroid -118.418E 38.519N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: tmi (extremity, regional percentile 0.96); rtp (extremity, regional percentile 0.96); geod_2ndinv (high, regional percentile 0.95); geod_dilaterate (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 131. candidate 445 (near_trace, confidence low-to-provisional)
+
+Closed group 445: 197 emitted px (2.4 km2), PCA axes 4.2 x 0.8 km, elongation 5.07, centroid -118.999E 40.329N. Measured principal-axis azimuth 20deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: tmi (extremity, regional percentile 0.09); cond_surf (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 9.8 px (18% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 132. candidate 681 (near_trace, confidence low)
+
+Closed group 681: 194 emitted px (3.7 km2), PCA axes 2.8 x 2.4 km, elongation 1.21, centroid -117.671E 40.203N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 1.4 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 133. candidate 940 (near_trace, confidence low)
+
+Closed group 940: 188 emitted px (3.0 km2), PCA axes 3.0 x 1.6 km, elongation 1.83, centroid -117.094E 40.084N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 4.1 px (43% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 134. candidate 395 (halo, confidence low-to-provisional)
+
+Closed group 395: 186 emitted px (2.6 km2), PCA axes 6.3 x 0.7 km, elongation 8.65, centroid -119.262E 40.353N. Measured principal-axis azimuth 15deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.92); grav_asa (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 135. candidate 200 (near_trace, confidence low-to-provisional)
+
+Closed group 200: 184 emitted px (2.6 km2), PCA axes 6.8 x 0.8 km, elongation 8.26, centroid -119.596E 40.525N. Measured principal-axis azimuth 40deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: rtp (extremity, regional percentile 0.07); grav_hgm (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (73% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 136. candidate 205 (halo, confidence low)
+
+Closed group 205: 184 emitted px (2.5 km2), PCA axes 6.1 x 0.7 km, elongation 8.76, centroid -117.418E 40.548N. Measured principal-axis azimuth 29deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 137. candidate 689 (halo, confidence low)
+
+Closed group 689: 182 emitted px (2.7 km2), PCA axes 6.8 x 0.7 km, elongation 9.90, centroid -119.508E 40.160N. Measured principal-axis azimuth 174deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 138. candidate 1089 (halo, confidence low-to-provisional)
+
+Closed group 1089: 178 emitted px (3.0 km2), PCA axes 3.1 x 1.5 km, elongation 2.09, centroid -119.018E 39.988N. Measured principal-axis azimuth 14deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 139. candidate 223 (near_trace, confidence low-to-provisional)
+
+Closed group 223: 178 emitted px (2.7 km2), PCA axes 5.6 x 0.9 km, elongation 6.07, centroid -117.561E 40.531N. Measured principal-axis azimuth 164deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (65% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 140. candidate 139 (near_trace, confidence low-to-provisional)
+
+Closed group 139: 177 emitted px (2.4 km2), PCA axes 5.6 x 1.2 km, elongation 4.49, centroid -118.116E 40.618N. Measured principal-axis azimuth 60deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: gravity_edge, seismicity. Strongest measured support: rtp (extremity, regional percentile 0.91); iso_grav_anom (extremity, regional percentile 0.96); grav_hgm (high, regional percentile 0.91); grav_asa (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.2 px (48% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 141. candidate 1798 (near_trace, confidence low-to-provisional)
+
+Closed group 1798: 177 emitted px (2.1 km2), PCA axes 5.7 x 0.7 km, elongation 8.05, centroid -118.400E 39.203N. Measured principal-axis azimuth 24deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_2ndinv (high, regional percentile 0.93); ieq_n100a15 (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (60% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 142. candidate 2130 (halo, confidence low-to-provisional)
+
+Closed group 2130: 176 emitted px (3.2 km2), PCA axes 5.7 x 1.2 km, elongation 4.89, centroid -118.207E 38.624N. Measured principal-axis azimuth 134deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.95); ieq_n100a15 (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 143. candidate 2502 (halo, confidence low)
+
+Closed group 2502: 176 emitted px (2.5 km2), PCA axes 4.4 x 0.9 km, elongation 4.61, centroid -117.371E 37.733N. Measured principal-axis azimuth 6deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 144. candidate 2533 (halo, confidence low-to-provisional)
+
+Closed group 2533: 175 emitted px (2.4 km2), PCA axes 5.2 x 1.6 km, elongation 3.22, centroid -117.967E 37.683N. Measured principal-axis azimuth 12deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.07); geod_2ndinv (high, regional percentile 0.98); geod_shearrate (high, regional percentile 0.96). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 145. candidate 1556 (halo, confidence low-to-provisional)
+
+Closed group 1556: 172 emitted px (3.0 km2), PCA axes 2.6 x 1.9 km, elongation 1.37, centroid -119.128E 39.446N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: mag_hgm (high, regional percentile 0.97); mag_asa (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 146. candidate 1284 (halo, confidence low)
+
+Closed group 1284: 172 emitted px (2.7 km2), PCA axes 3.0 x 1.4 km, elongation 2.12, centroid -119.357E 39.760N. Measured principal-axis azimuth 148deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 147. candidate 618 (halo, confidence low)
+
+Closed group 618: 169 emitted px (2.7 km2), PCA axes 2.9 x 1.5 km, elongation 1.88, centroid -119.447E 40.215N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 148. candidate 1919 (halo, confidence low-to-provisional)
+
+Closed group 1919: 166 emitted px (2.7 km2), PCA axes 3.6 x 1.9 km, elongation 1.88, centroid -118.145E 39.037N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 2/6 signal families reach the fault-favourable regional decile: seismicity, strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.91); ieq_n100a15 (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 149. candidate 119 (near_trace, confidence low)
+
+Closed group 119: 165 emitted px (2.7 km2), PCA axes 3.3 x 2.3 km, elongation 1.43, centroid -117.227E 40.647N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.8 px (58% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 150. candidate 399 (near_trace, confidence low-to-provisional)
+
+Closed group 399: 164 emitted px (2.3 km2), PCA axes 4.2 x 0.9 km, elongation 4.85, centroid -118.856E 40.359N. Measured principal-axis azimuth 3deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: conductivity, seismicity. Strongest measured support: cond_surf (high, regional percentile 0.99); deq_n100a15 (low, regional percentile 0.05). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 151. candidate 1349 (halo, confidence low-to-provisional)
+
+Closed group 1349: 163 emitted px (2.7 km2), PCA axes 2.1 x 1.9 km, elongation 1.12, centroid -119.192E 39.686N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.00); mag_hgm (high, regional percentile 0.97); mag_asa (high, regional percentile 0.97); rtp (extremity, regional percentile 0.00). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 152. candidate 1597 (halo, confidence low-to-provisional)
+
+Closed group 1597: 161 emitted px (2.4 km2), PCA axes 4.7 x 1.1 km, elongation 4.24, centroid -117.938E 39.415N. Measured principal-axis azimuth 178deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.06); geod_dilaterate (high, regional percentile 0.95). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 153. candidate 1699 (near_trace, confidence low-to-provisional)
+
+Closed group 1699: 161 emitted px (2.2 km2), PCA axes 3.5 x 1.1 km, elongation 3.32, centroid -118.128E 39.332N. Measured principal-axis azimuth 28deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_shearrate (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 4.1 px (45% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 154. candidate 1222 (near_trace, confidence low-to-provisional)
+
+Closed group 1222: 159 emitted px (2.7 km2), PCA axes 5.0 x 1.2 km, elongation 4.26, centroid -119.743E 39.816N. Measured principal-axis azimuth 126deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: geod_dilaterate (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (64% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 155. candidate 1094 (near_trace, confidence low-to-provisional)
+
+Closed group 1094: 159 emitted px (2.0 km2), PCA axes 6.4 x 0.7 km, elongation 9.34, centroid -117.855E 39.995N. Measured principal-axis azimuth 38deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: slope_break. Strongest measured support: slope_of_slope (high, regional percentile 0.90). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.0 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 156. candidate 1607 (halo, confidence low)
+
+Closed group 1607: 157 emitted px (2.8 km2), PCA axes 3.9 x 1.1 km, elongation 3.46, centroid -119.320E 39.390N. Measured principal-axis azimuth 25deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 157. candidate 221 (halo, confidence low-to-provisional)
+
+Closed group 221: 154 emitted px (2.5 km2), PCA axes 5.7 x 1.2 km, elongation 4.61, centroid -118.998E 40.515N. Measured principal-axis azimuth 9deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: conductivity. Strongest measured support: cond_surf (high, regional percentile 0.98). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 158. candidate 910 (near_trace, confidence low)
+
+Closed group 910: 154 emitted px (2.4 km2), PCA axes 7.2 x 1.4 km, elongation 5.18, centroid -116.872E 40.098N. Measured principal-axis azimuth 50deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.0 px (61% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 159. candidate 2488 (near_trace, confidence low-to-provisional)
+
+Closed group 2488: 153 emitted px (2.1 km2), PCA axes 3.0 x 1.2 km, elongation 2.47, centroid -117.982E 37.768N. Measured principal-axis azimuth 66deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 2/6 signal families reach the fault-favourable regional decile: conductivity, strain. Strongest measured support: iso_grav_anom (extremity, regional percentile 0.04); det_elev (extremity, regional percentile 0.04); geod_2ndinv (high, regional percentile 0.95); geod_shearrate (high, regional percentile 0.97). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (53% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 160. candidate 5 (halo, confidence low)
+
+Closed group 5: 151 emitted px (2.0 km2), PCA axes 4.8 x 1.0 km, elongation 5.04, centroid -118.896E 40.689N. Measured principal-axis azimuth 22deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 161. candidate 2191 (near_trace, confidence low)
+
+Closed group 2191: 147 emitted px (3.1 km2), PCA axes 3.2 x 1.9 km, elongation 1.67, centroid -117.886E 38.538N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 3.2 px (49% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 162. candidate 406 (near_trace, confidence low-to-provisional)
+
+Closed group 406: 146 emitted px (2.5 km2), PCA axes 3.1 x 1.3 km, elongation 2.44, centroid -116.978E 40.374N. Measured principal-axis azimuth 25deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.93). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.8 px (60% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 163. candidate 271 (halo, confidence low-to-provisional)
+
+Closed group 271: 145 emitted px (2.1 km2), PCA axes 5.4 x 0.6 km, elongation 8.66, centroid -118.055E 40.474N. Measured principal-axis azimuth 0deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_asa (high, regional percentile 0.99); depth_to_base_surf (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 164. candidate 728 (isolated, confidence low)
+
+Closed group 728: 143 emitted px (2.1 km2), PCA axes 3.3 x 1.0 km, elongation 3.44, centroid -117.041E 40.186N. Measured principal-axis azimuth 7deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 7.2 px (9% of emitted pixels within 300 m): the farthest-off population. Isolation raises interest and, equally, the false-positive risk - isolation alone has no confirmatory weight. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 165. candidate 316 (halo, confidence low)
+
+Closed group 316: 142 emitted px (2.4 km2), PCA axes 4.3 x 1.1 km, elongation 3.97, centroid -118.107E 40.439N. Measured principal-axis azimuth 2deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 166. candidate 167 (halo, confidence low)
+
+Closed group 167: 142 emitted px (2.1 km2), PCA axes 6.0 x 1.3 km, elongation 4.61, centroid -119.087E 40.566N. Measured principal-axis azimuth 7deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 167. candidate 878 (near_trace, confidence low)
+
+Closed group 878: 140 emitted px (2.4 km2), PCA axes 6.8 x 1.0 km, elongation 6.79, centroid -116.933E 40.108N. Measured principal-axis azimuth 25deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 1.4 px (74% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 168. candidate 532 (halo, confidence low)
+
+Closed group 532: 139 emitted px (2.2 km2), PCA axes 6.7 x 0.8 km, elongation 8.78, centroid -119.431E 40.262N. Measured principal-axis azimuth 15deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 169. candidate 463 (halo, confidence low)
+
+Closed group 463: 138 emitted px (2.2 km2), PCA axes 2.5 x 1.4 km, elongation 1.82, centroid -118.718E 40.334N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 170. candidate 282 (near_trace, confidence low-to-provisional)
+
+Closed group 282: 137 emitted px (2.2 km2), PCA axes 2.6 x 1.4 km, elongation 1.83, centroid -116.877E 40.482N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. 1/6 signal families reach the fault-favourable regional decile: gravity_edge. Strongest measured support: grav_hgm (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (69% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 171. candidate 1155 (near_trace, confidence low-to-provisional)
+
+Closed group 1155: 136 emitted px (2.1 km2), PCA axes 2.8 x 1.0 km, elongation 2.90, centroid -119.077E 39.893N. Measured principal-axis azimuth 0deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.10); mag_hgm (high, regional percentile 0.92); mag_asa (high, regional percentile 0.92). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.5 px (66% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 172. candidate 521 (near_trace, confidence low-to-provisional)
+
+Closed group 521: 133 emitted px (2.9 km2), PCA axes 5.7 x 1.0 km, elongation 5.75, centroid -119.067E 40.276N. Measured principal-axis azimuth 26deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: seismicity. Strongest measured support: deq_n100a15 (low, regional percentile 0.03). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 2.2 px (64% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 173. candidate 1341 (halo, confidence low-to-provisional)
+
+Closed group 1341: 130 emitted px (2.9 km2), PCA axes 3.8 x 1.9 km, elongation 2.02, centroid -118.991E 39.709N. Measured principal-axis azimuth 73deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: tmi (extremity, regional percentile 0.04); mag_hgm (high, regional percentile 0.91); mag_asa (high, regional percentile 0.91). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 174. candidate 1998 (halo, confidence low-to-provisional)
+
+Closed group 1998: 129 emitted px (2.0 km2), PCA axes 3.8 x 0.9 km, elongation 4.06, centroid -118.778E 38.834N. Measured principal-axis azimuth 164deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: strain. Strongest measured support: rtp (extremity, regional percentile 0.08); det_elev (extremity, regional percentile 0.07); geod_2ndinv (high, regional percentile 0.94); geod_shearrate (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. This group lies essentially ON the mapped catalogue (median 2.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 175. candidate 2016 (near_trace, confidence low)
+
+Closed group 2016: 126 emitted px (2.1 km2), PCA axes 6.6 x 0.7 km, elongation 9.30, centroid -117.843E 38.797N. Measured principal-axis azimuth 165deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 1.4 px (75% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 176. candidate 1363 (near_trace, confidence low)
+
+Closed group 1363: 120 emitted px (2.1 km2), PCA axes 3.7 x 1.5 km, elongation 2.40, centroid -119.290E 39.665N. Measured principal-axis azimuth 33deg - NE (accommodation/cross-grain): in the orientation of the northeast-striking accommodation structures reported along the margins of the Walker Lane. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). Median distance to the catalogue is 2.1 px (72% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 177. candidate 711 (halo, confidence low)
+
+Closed group 711: 119 emitted px (2.4 km2), PCA axes 2.9 x 1.6 km, elongation 1.86, centroid -118.312E 40.188N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 178. candidate 457 (halo, confidence low)
+
+Closed group 457: 108 emitted px (2.0 km2), PCA axes 3.1 x 1.9 km, elongation 1.59, centroid -116.509E 40.345N. The body is too equant for its principal axis to be called a lineation; treat any azimuth reading below as weak. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.4 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 179. candidate 1484 (near_trace, confidence low-to-provisional)
+
+Closed group 1484: 101 emitted px (2.1 km2), PCA axes 3.2 x 1.3 km, elongation 2.45, centroid -119.244E 39.511N. Measured principal-axis azimuth 128deg - outside the three regional trend families: not aligned with any of the three cited regional trend families; in this province such an orientation (E-W through SW) is least specific and should not be promoted on trend alone. This is a kinematic consistency statement, not an identification of faulting style. 1/6 signal families reach the fault-favourable regional decile: magnetic_edge. Strongest measured support: mag_hgm (high, regional percentile 0.94); mag_asa (high, regional percentile 0.94). These families co-vary (measured strain|seismicity Spearman 0.77 at catalogue pixels - feature_priorities_audit_2026-09-26.json), so n families is not n independent confirmations. Median distance to the catalogue is 3.2 px (48% of emitted pixels within 300 m): close enough to a mapped trace that the model may be thickening or bridging known structure. New-fault truth may include corrections within 300 m of mapped faults, but this group cannot be counted as an independent discovery. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
+### 180. candidate 2545 (halo, confidence low)
+
+Closed group 2545: 98 emitted px (2.1 km2), PCA axes 4.4 x 1.0 km, elongation 4.53, centroid -117.473E 37.664N. Measured principal-axis azimuth 16deg - N (Basin-and-Range normal): consistent with the northerly-trending high-angle normal faults that characterise Basin-and-Range extension in this part of Nevada. This is a kinematic consistency statement, not an identification of faulting style. No family reaches the fault-favourable regional decile (0/6; magnetic tilt and its ratio are excluded from voting - see tilt_derivative_audit.json). This group lies essentially ON the mapped catalogue (median 1.0 px): under the staff masking rule these pixels sit inside the excluded training-label band, so their score contribution is masked rather than evidence of a hidden fault. Depth: not estimable from the supplied bands - the provided magnetic vertical gradient is ~230x smaller than its horizontal counterpart, so the +/-45 deg tilt-depth contours do not exist on these bytes (tilt_derivative_audit.json). No depth is claimed. Counterinterpretation: at 100 m raster resolution a compact group like this can be produced by a detrended-elevation curvature artefact, a lithological contact, or local noise; none of these is excluded by the measured diagnostics.
+
diff --git a/index.html b/index.html
index 97b6579..78bf859 100644
--- a/index.html
+++ b/index.html
@@ -23,6 +23,11 @@ GeoDAWN region of Nevada/California.</div>
     <span class="meta">1,652,883 bytes · sha256 <code>33cec71ff00b3f32d0d59c81c156f3f1…</code></span>
   </p>
   <ol class="steps">
+    <li><strong>Before any upload</strong> (account holder, with credentials):
+        confirm the one DrivenData registration, its §1.3 eligibility and the
+        remaining weekly allowance, and confirm the §3.2 generative-AI disclosure
+        is written — see <code>SUBMISSION_GUIDE.md &sect;0</code>. None of that is
+        knowable from this sandbox.</li>
     <li>Download the file above (one click).</li>
     <li>Open the <a href="https://www.drivendata.org/competitions/306/competition-doe-gems/" rel="noopener">competition page</a> and click
         <em>Submit</em> → <em>Make new submission</em> (DrivenData sign-in required).</li>
@@ -40,8 +45,11 @@ GeoDAWN region of Nevada/California.</div>
     (300 m buffer). The submission keeps the top 3% of the
     footprint by predicted probability and writes 1.0 on those pixels: the published
     metric reduces to DTI = TP_w/(0.8*n_gt + 0.2*FP_w + 0.2*TP_w), which is strictly
-    increasing in the predicted value, so fractional confidence gives score away. The
-    budget is the minimax-regret choice across ground-truth sizes (experiments card).
+    increasing under uniform rescaling of every predicted value, and the hard 1.0
+    mask on the selected top-k was the measured winner on blocked folds (an
+    empirical catalogue policy, not a claim that every fractional value should be
+    1). The budget is the minimax-regret choice across ground-truth sizes
+    (experiments card).
     Blocked-CV proxy DTI 0.1698 against the public catalogue — a
     proxy, not a leaderboard value. Format verified by
     scripts/validate_submission.py (13/13 checks incl. NaN-inside-footprint).</code></p>
@@ -137,11 +145,16 @@ GeoDAWN region of Nevada/California.</div>
   <pre class="code">DTI = TP_w / ( TP_w + α·FP_w + β·FN_w )
     = TP_w / ( β·n_gt + α·FP_w + (1−β)·TP_w )          α=0.2, β=0.8</pre>
   <p>The <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is
-  <strong>strictly increasing</strong> under <code>p → λ·p</code> for every λ up to the
-  cap at 1. Measured, on the configuration the previous file used:</p>
+  <strong>strictly increasing</strong> under <code>p → λ·p</code> — that is, under
+  <em>uniform rescaling of every predicted value</em> — for every λ up to the cap at
+  1: a uniformly shrunk submission donates score. Hardening each selected value to
+  1 is the separate, empirical policy, measured on the configuration the previous
+  file used:</p>
   <table class=""><thead><tr><th>Placement</th><th>mean blocked DTI</th></tr></thead><tbody><tr><td><code>soft@0.3</code></td><td>0.1119</td></tr><tr><td><code>hard@0.3</code></td><td>0.1648</td></tr><tr><td><code>topk_hard@0.03</code></td><td>0.1682</td></tr><tr><td><code>topk_hard@0.05</code></td><td>0.1730</td></tr></tbody></table>
-  <p>Fractional confidence was donating roughly a third of the achievable score to
-  nobody.</p>
+  <p>On these folds the fractional surface was leaving roughly a third of the
+  achievable score unclaimed. (The uniform-scaling identity does not by itself prove
+  that any particular hardening helps: a high-confidence remote false positive is
+  charged the full <code>α</code> whether it was 0.01 or 1.0 in the soft surface.)</p>
   <h3>2. Choose a budget, and make it robust to how big the test set is</h3>
   <p><code>FP_w</code> is an <em>absolute</em> sum over predicted pixels, while
   <code>TP_w</code> and <code>FN_w</code> are per-ground-truth-pixel sums. The right
diff --git a/research.html b/research.html
index 2f1756b..0af8ba0 100644
--- a/research.html
+++ b/research.html
@@ -30,12 +30,16 @@ GeoDAWN region of Nevada/California.</div>
     <li>Densifying is cheap <em>where a fault really is</em> — a false positive one
       pixel off the trace is charged <code>α·(1 − 2/3) = 0.067</code>, not
       <code>α = 0.2</code> — so a confident trace can be painted generously.</li>
-    <li>But it is only cheap there. Measured, dilation loses: the
+    <li>But it is only cheap there. On the catalogue-CV axis, dilation loses: the
       <code>densified</code> placement scored 0.0803 against 0.0879 for the same
       surface left alone, and the lineament-enhanced surfaces below lose more. Most
       pixels a dilation adds are <em>not</em> near a fault, and those are charged the
       full <code>α</code>. The algebra is a statement about pixels adjacent to a true
-      fault; it is not a licence to thicken the whole map.</li>
+      fault; it is not a licence to thicken the whole map. (That is a statement
+      about <em>this</em> catalogue proxy at <em>this</em> budget — a later SGMC
+      new-fault-proxy study found the opposite direction on its proxy but failed its
+      own no-skill control, so neither result generalises; masks and budgets must
+      be stated with any dilation claim.)</li>
     <li>Thinning a true line to every 4th–5th pixel <em>loses</em> score rather than
       trading it away cleanly: skipped ground-truth pixels are then served at ≥ 2 px,
       where k ≤ 1/3, and β = 0.8 makes misses expensive. The 300 m kernel implies
@@ -50,11 +54,13 @@ GeoDAWN region of Nevada/California.</div>
   <p>Three consequences, all of them measured in
   <code>scripts/experiment.py</code> rather than asserted:</p>
   <ol>
-    <li><strong>The submitted value should be 1.0 on the pixels you select.</strong>
-      The <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is
-      strictly increasing under <code>p → λ·p</code> for every λ up to the cap. Writing
-      the model's fractional confidence instead measured 0.1119 against 0.1648 for
-      the identical selection at threshold 0.30.</li>
+    <li><strong>On these folds, 1.0 on the selected pixels beat the model's
+      fractional probability.</strong> The provable statement is narrower: the
+      <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is strictly
+      increasing under <code>p → λ·p</code> — uniform rescaling of every value.
+      Writing the model's fractional confidence instead measured 0.1119 against
+      0.1648 for the identical selection at threshold 0.30; that is a catalogue-CV
+      measurement of one hardening policy, not a theorem about all of them.</li>
     <li><strong>Control the budget, not the threshold.</strong> <code>FP_w</code> is an
       absolute sum over predicted pixels; <code>TP_w</code> and <code>FN_w</code> are
       per-ground-truth-pixel sums. The optimum therefore depends on how many fault
diff --git a/scripts/build_features.py b/scripts/build_features.py
index 3548c30..62c0877 100644
--- a/scripts/build_features.py
+++ b/scripts/build_features.py
@@ -3,8 +3,10 @@
 
     python scripts/build_features.py [--tile-rows 512] [--out data/evidence/features.f32.npy]
 
-Why streaming: the full stack is 12,279,160 px × 48 channels × 4 B ≈ 2.25 GB, and
-this host has 3 GB of RAM. An earlier full-grid version of this script thrashed
+Why streaming: the full stack is 12,279,160 px × 88 channels × 4 B ≈ 4.32 GB
+(4,322,264,320 bytes), and this host has 3 GB of RAM. (An earlier revision of
+this comment said "48 channels ≈ 2.25 GB" — stale after the extended channels
+were appended.) An earlier full-grid version of this script thrashed
 against the memory limit and took ~3 minutes per channel; processing row tiles with
 a halo and writing the interior keeps peak memory near 200 MB and finishes the whole
 stack in a few minutes.
diff --git a/scripts/build_site.py b/scripts/build_site.py
index 18eee14..e88719c 100644
--- a/scripts/build_site.py
+++ b/scripts/build_site.py
@@ -199,6 +199,11 @@ def submission_card() -> str:
     <span class="meta">{rep['bytes']:,} bytes · sha256 <code>{esc(rep['sha256'][:32])}…</code></span>
   </p>
   <ol class="steps">
+    <li><strong>Before any upload</strong> (account holder, with credentials):
+        confirm the one DrivenData registration, its §1.3 eligibility and the
+        remaining weekly allowance, and confirm the §3.2 generative-AI disclosure
+        is written — see <code>SUBMISSION_GUIDE.md &sect;0</code>. None of that is
+        knowable from this sandbox.</li>
     <li>Download the file above (one click).</li>
     <li>Open the {link(spec.URLS['home'], 'competition page')} and click
         <em>Submit</em> → <em>Make new submission</em> (DrivenData sign-in required).</li>
@@ -216,8 +221,11 @@ def submission_card() -> str:
     (300 m buffer). The submission keeps the top {esc(shipped_budget_pct())} of the
     footprint by predicted probability and writes 1.0 on those pixels: the published
     metric reduces to DTI = TP_w/(0.8*n_gt + 0.2*FP_w + 0.2*TP_w), which is strictly
-    increasing in the predicted value, so fractional confidence gives score away. The
-    budget is the minimax-regret choice across ground-truth sizes (experiments card).
+    increasing under uniform rescaling of every predicted value, and the hard 1.0
+    mask on the selected top-k was the measured winner on blocked folds (an
+    empirical catalogue policy, not a claim that every fractional value should be
+    1). The budget is the minimax-regret choice across ground-truth sizes
+    (experiments card).
     Blocked-CV proxy DTI {esc(shipped_cv_dti())} against the public catalogue — a
     proxy, not a leaderboard value. Format verified by
     scripts/validate_submission.py (13/13 checks incl. NaN-inside-footprint).</code></p>
@@ -421,11 +429,16 @@ def experiments_card() -> str:
   <pre class="code">DTI = TP_w / ( TP_w + α·FP_w + β·FN_w )
     = TP_w / ( β·n_gt + α·FP_w + (1−β)·TP_w )          α=0.2, β=0.8</pre>
   <p>The <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is
-  <strong>strictly increasing</strong> under <code>p → λ·p</code> for every λ up to the
-  cap at 1. Measured, on the configuration the previous file used:</p>
+  <strong>strictly increasing</strong> under <code>p → λ·p</code> — that is, under
+  <em>uniform rescaling of every predicted value</em> — for every λ up to the cap at
+  1: a uniformly shrunk submission donates score. Hardening each selected value to
+  1 is the separate, empirical policy, measured on the configuration the previous
+  file used:</p>
   {table(['Placement', 'mean blocked DTI'], head)}
-  <p>Fractional confidence was donating roughly a third of the achievable score to
-  nobody.</p>
+  <p>On these folds the fractional surface was leaving roughly a third of the
+  achievable score unclaimed. (The uniform-scaling identity does not by itself prove
+  that any particular hardening helps: a high-confidence remote false positive is
+  charged the full <code>α</code> whether it was 0.01 or 1.0 in the soft surface.)</p>
   <h3>2. Choose a budget, and make it robust to how big the test set is</h3>
   <p><code>FP_w</code> is an <em>absolute</em> sum over predicted pixels, while
   <code>TP_w</code> and <code>FN_w</code> are per-ground-truth-pixel sums. The right
@@ -1018,12 +1031,16 @@ def build_research() -> str:
     <li>Densifying is cheap <em>where a fault really is</em> — a false positive one
       pixel off the trace is charged <code>α·(1 − 2/3) = 0.067</code>, not
       <code>α = 0.2</code> — so a confident trace can be painted generously.</li>
-    <li>But it is only cheap there. Measured, dilation loses: the
+    <li>But it is only cheap there. On the catalogue-CV axis, dilation loses: the
       <code>densified</code> placement scored 0.0803 against 0.0879 for the same
       surface left alone, and the lineament-enhanced surfaces below lose more. Most
       pixels a dilation adds are <em>not</em> near a fault, and those are charged the
       full <code>α</code>. The algebra is a statement about pixels adjacent to a true
-      fault; it is not a licence to thicken the whole map.</li>
+      fault; it is not a licence to thicken the whole map. (That is a statement
+      about <em>this</em> catalogue proxy at <em>this</em> budget — a later SGMC
+      new-fault-proxy study found the opposite direction on its proxy but failed its
+      own no-skill control, so neither result generalises; masks and budgets must
+      be stated with any dilation claim.)</li>
     <li>Thinning a true line to every 4th–5th pixel <em>loses</em> score rather than
       trading it away cleanly: skipped ground-truth pixels are then served at ≥ 2 px,
       where k ≤ 1/3, and β = 0.8 makes misses expensive. The 300 m kernel implies
@@ -1038,11 +1055,13 @@ def build_research() -> str:
   <p>Three consequences, all of them measured in
   <code>scripts/experiment.py</code> rather than asserted:</p>
   <ol>
-    <li><strong>The submitted value should be 1.0 on the pixels you select.</strong>
-      The <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is
-      strictly increasing under <code>p → λ·p</code> for every λ up to the cap. Writing
-      the model's fractional confidence instead measured 0.1119 against 0.1648 for
-      the identical selection at threshold 0.30.</li>
+    <li><strong>On these folds, 1.0 on the selected pixels beat the model's
+      fractional probability.</strong> The provable statement is narrower: the
+      <code>β·n_gt</code> term does not scale with <code>p</code>, so DTI is strictly
+      increasing under <code>p → λ·p</code> — uniform rescaling of every value.
+      Writing the model's fractional confidence instead measured 0.1119 against
+      0.1648 for the identical selection at threshold 0.30; that is a catalogue-CV
+      measurement of one hardening policy, not a theorem about all of them.</li>
     <li><strong>Control the budget, not the threshold.</strong> <code>FP_w</code> is an
       absolute sum over predicted pixels; <code>TP_w</code> and <code>FN_w</code> are
       per-ground-truth-pixel sums. The optimum therefore depends on how many fault
diff --git a/src/gems/cv.py b/src/gems/cv.py
index 41a6eca..4f997e6 100644
--- a/src/gems/cv.py
+++ b/src/gems/cv.py
@@ -12,9 +12,9 @@ Design:
   * Folds are formed by dealing blocks in a strided pattern so that each fold's
     blocks are spread over the region rather than clustered in one corner.
   * A BUFFER of `buffer_px` pixels around every held-out block is also held out
-    of *training* (so no training pixel is within the kernel support of a scored
-    pixel) and excluded from *scoring* (so the scored pixels are not influenced
-    by test-block edges).
+    of *training* using a Euclidean disk (so no training pixel is within the
+    300 m kernel support of a scored pixel) and excluded from *scoring* (so the
+    scored pixels are not influenced by test-block edges).
   * Because the target is unmapped faults, we also report the score with the
     catalogued faults removed from the ground truth inside the held-out blocks
     ("gap score"), which is the closest honest proxy available without the
@@ -26,6 +26,7 @@ from __future__ import annotations
 from dataclasses import dataclass
 
 import numpy as np
+from scipy import ndimage
 
 
 @dataclass
@@ -40,7 +41,7 @@ class Folds:
         """Boolean (train, score) masks for fold k.
 
         `train` excludes the held-out blocks *and* their buffer, so no training
-        pixel lies within the kernel support of a scored pixel. `score` is the
+        pixel lies within the Euclidean kernel support of a scored pixel. `score` is the
         held-out blocks themselves — the buffer is not scored either, because a
         pixel next to the train/test boundary is contaminated by whichever side
         the model learned from.
@@ -60,17 +61,17 @@ class Folds:
 
 
 def _dilate(mask: np.ndarray, iterations: int) -> np.ndarray:
+    """Dilation by a Euclidean disk, matching the metric's pixel-radius geometry.
+
+    A four-neighbour iterated cross uses Manhattan distance and misses the (2, 2)
+    corner at a 3 px radius: sqrt(8) < 3. That corner is still inside the
+    official 300 m kernel and must not be in training.
+    """
     if iterations <= 0:
         return mask
-    out = mask.copy()
-    for _ in range(iterations):
-        shifted = out.copy()
-        shifted[1:, :] |= out[:-1, :]
-        shifted[:-1, :] |= out[1:, :]
-        shifted[:, 1:] |= out[:, :-1]
-        shifted[:, :-1] |= out[:, 1:]
-        out = shifted
-    return out
+    yy, xx = np.ogrid[-iterations:iterations + 1, -iterations:iterations + 1]
+    disk = xx * xx + yy * yy <= iterations * iterations
+    return ndimage.binary_dilation(mask, structure=disk)
 
 
 def make_folds(n_blocks: int = 4, n_folds: int = 4, buffer_px: int = 3) -> Folds:
diff --git a/tests/test_spec_and_cv.py b/tests/test_spec_and_cv.py
index 0632048..b158dee 100644
--- a/tests/test_spec_and_cv.py
+++ b/tests/test_spec_and_cv.py
@@ -51,16 +51,46 @@ def test_blocked_folds_have_a_buffer_between_train_and_score():
     shape = (80, 80)
     train, score = f.train_test_masks(shape, 0)
     assert score.any() and (~score).any()
-    # every scored pixel is at least buffer_px+1 away from any training pixel
-    rows = np.flatnonzero(score.any(axis=1))
-    for r in rows:
-        assert not train[r].any() or True
+    # every scored pixel is more than buffer_px (Euclidean) away from any
+    # training pixel. Measured with an INDEPENDENT oracle (distance transform),
+    # not with cv._dilate itself: a test that reuses the function under test
+    # cannot detect a buffer-geometry bug. (An earlier revision asserted
+    # `not train[r].any() or True`, which is always true and proved nothing.)
+    from scipy import ndimage
+    dist = ndimage.distance_transform_edt(~train)
+    assert dist[score].min() > 3.0
     # specifically: train mask must be a strict subset of the complement of the
     # dilated score mask
     dilated = cv._dilate(score, 3)
     assert not (train & dilated).any()
 
 
+def test_buffer_is_euclidean_at_diagonal_corners():
+    """The (2,2) corner neighbour is sqrt(8) ~ 2.83 px = 283 m from the held-out
+    block: inside the official 300 m kernel, so it must NOT be in training.
+
+    This layout (one held-out block, training diagonally adjacent) is the
+    minimal case the old Manhattan-diamond buffer got wrong; the historical
+    4x4/6x6 strided layouts never expose it because other held-out blocks cover
+    each other's corners, which is why the bug was latent, not historical.
+    """
+    from scipy import ndimage
+    f = cv.Folds(fold_of_block=np.array([[0, 1], [1, 1]]), n_folds=2,
+                 n_blocks=2, buffer_px=3)
+    shape = (80, 80)
+    train, score = f.train_test_masks(shape, 0)
+    assert score[:40, :40].all() and not score[40:].any()
+    dist = ndimage.distance_transform_edt(~train)
+    assert dist[score].min() > 3.0, (
+        f"training pixel within 300 m of a scored pixel "
+        f"(min Euclidean distance {dist[score].min():.3f} px)")
+    # and the (2,2) offset from the block corner (39,39) -> (41,41) itself,
+    # at sqrt(8) ~ 2.83 px, must be excluded from training
+    assert not train[41, 41]
+    # while the (3,3) offset at sqrt(18) ~ 4.24 px is legitimately trainable
+    assert train[42, 42]
+
+
 def test_folds_partition_the_grid():
     f = cv.make_folds(n_blocks=4, n_folds=4, buffer_px=3)
     shape = (64, 64)
-- 
2.39.5


From 57c4bd827501e1c8c2ed3552cd28d6f6f207180a Mon Sep 17 00:00:00 2001
From: "arena-ai-coding-agent[bot]"
 <298482267+arena-ai-coding-agent[bot]@users.noreply.github.com>
Date: Sat, 26 Sep 2026 17:02:42 +0000
Subject: [PATCH 2/2] Session re-verification 2026-09-26T1702Z: gate 13/13,
 hashes 3/3, pytest 47/47, site rebuild drift-free on the patched tree

---
 ...ssion_reverification_2026-09-26T1702Z.json | 155 ++++++++++++++++++
 1 file changed, 155 insertions(+)
 create mode 100644 data/evidence/session_reverification_2026-09-26T1702Z.json

diff --git a/data/evidence/session_reverification_2026-09-26T1702Z.json b/data/evidence/session_reverification_2026-09-26T1702Z.json
new file mode 100644
index 0000000..f662a96
--- /dev/null
+++ b/data/evidence/session_reverification_2026-09-26T1702Z.json
@@ -0,0 +1,155 @@
+{
+  "session": "2026-09-26T1702Z",
+  "generated_utc": "2026-09-26T17:02:37Z",
+  "branch": "arena/01a0de93-6gemsdoe",
+  "note": "Session re-verification: gate 13/13 PASS, official rasters 3/3 hash-verified, pytest 47/47 PASS, site rebuild drift-free, account-status re-audited (11 GEMS-named repos, Pages built on 11 (still flagged)).",
+  "submission_file": {
+    "path": "downloads/gems6_hgb88-topk03_33cec71ff0.tif",
+    "sha256": "33cec71ff00b3f32d0d59c81c156f3f1488ffef46baa4b6499094e24ea1875ab",
+    "sha256_matches_expected": true,
+    "expected_sha256": "33cec71ff00b3f32d0d59c81c156f3f1488ffef46baa4b6499094e24ea1875ab",
+    "bytes": 1652883,
+    "gate": "13/13 PASS",
+    "gate_ok": true,
+    "nan_inside_footprint": 0,
+    "finite_outside_footprint": 0,
+    "positive_pixels": 155021,
+    "finite_min": 0.0,
+    "finite_max": 1.0,
+    "strategy": "topk_hard@0.03 (HistGradientBoosting, 88 channels)"
+  },
+  "data_verification": {
+    "ok": true,
+    "files": {
+      "training_features.tif": {
+        "path": "data/training_features.tif",
+        "exists": true,
+        "bytes": 418912844,
+        "expected_bytes": 418912844,
+        "bytes_ok": true,
+        "sha256": "4371c82e3b8339b807bdffcf4ef59a225520fe2988d521be208ae33743123bc5",
+        "expected_sha256": "4371c82e3b8339b807bdffcf4ef59a225520fe2988d521be208ae33743123bc5",
+        "sha_ok": true,
+        "status": "verified"
+      },
+      "labels.tif": {
+        "path": "data/labels.tif",
+        "exists": true,
+        "bytes": 425830,
+        "expected_bytes": 425830,
+        "bytes_ok": true,
+        "sha256": "7ba308ccdc4418b31a178f4f1ef21aaa6e152e4028f2f6f64b01f7eb25ae4093",
+        "expected_sha256": "7ba308ccdc4418b31a178f4f1ef21aaa6e152e4028f2f6f64b01f7eb25ae4093",
+        "sha_ok": true,
+        "status": "verified"
+      },
+      "sample_submission.tif": {
+        "path": "data/sample_submission.tif",
+        "exists": true,
+        "bytes": 1599597,
+        "expected_bytes": 1599597,
+        "bytes_ok": true,
+        "sha256": "2176d08e485aa2cd2860ce8df539db4faf4d76163b38a4dd8c30a40454d35cbc",
+        "expected_sha256": "2176d08e485aa2cd2860ce8df539db4faf4d76163b38a4dd8c30a40454d35cbc",
+        "sha_ok": true,
+        "status": "verified"
+      }
+    }
+  },
+  "tests": {
+    "total": 47,
+    "passed": 47,
+    "failed": 0,
+    "pytest_exit_code": 0,
+    "pytest_summary_line": "47 passed in 6.22s",
+    "suites": [
+      "test_gate.py",
+      "test_metric.py",
+      "test_spec_and_cv.py"
+    ]
+  },
+  "site_build": {
+    "build_exit_code": 0,
+    "build_ok": true,
+    "drift_free": true,
+    "drifted_files": "",
+    "note": "scripts/build_site.py re-ran and the committed HTML was byte-identical (no drift)"
+  },
+  "account_status": {
+    "canonical_repo": "6GEMSDOE",
+    "canonical_site": "https://buffedlizard55-lab.github.io/6GEMSDOE/",
+    "duplication_flag": true,
+    "note": "STILL FLAGGED: the account holds 11 GEMS-named repositories; GitHub Pages is enabled and built on all 11. This repository is the single designated entry; no other copy is used for submissions or scoring.",
+    "gems_repos": [
+      {
+        "name": "11GEMSDOE",
+        "disk_usage_kb": 0,
+        "pages_status": "built"
+      },
+      {
+        "name": "5GEMSDOE",
+        "disk_usage_kb": 460609,
+        "pages_status": "built"
+      },
+      {
+        "name": "6GEMSDOE",
+        "disk_usage_kb": 390407,
+        "pages_status": "built"
+      },
+      {
+        "name": "7GEMSDOE",
+        "disk_usage_kb": 0,
+        "pages_status": "built"
+      },
+      {
+        "name": "8GEMSDOE",
+        "disk_usage_kb": 0,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE",
+        "disk_usage_kb": 400811,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE10",
+        "disk_usage_kb": 0,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE2",
+        "disk_usage_kb": 421640,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE3",
+        "disk_usage_kb": 24765,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE4",
+        "disk_usage_kb": 131419,
+        "pages_status": "built"
+      },
+      {
+        "name": "GEMSDOE9",
+        "disk_usage_kb": 0,
+        "pages_status": "built"
+      }
+    ],
+    "n_duplicate_repos": 10,
+    "action_required": "The sha256-pinned data bridge (419 MB in parts under data/bridge/) is now committed directly to 6GEMSDOE, so scripts/fetch_and_verify_data.py no longer depends on buffedlizard55-lab/GEMSDOE codeload. The other ten repositories can now safely be archived or deleted by the account holder."
+  },
+  "blocked_cv_proxy": {
+    "shipped_file_mean_blocked_dti": 0.1698,
+    "previous_file_mean_blocked_dti": 0.1119,
+    "note": "Proxy scores against the public catalogue on spatially blocked, buffered folds (4x4 blocks, 300 m buffer). Not leaderboard values: the leaderboard scores faults missing from the catalogue."
+  },
+  "limitations_and_blockers": [
+    "No DrivenData credentials in this sandbox: cannot read the public leaderboard, cannot submit, cannot download the official data tab directly. The official bytes are carried as sha256-pinned parts committed under data/bridge/ in THIS repo and re-verified on every placement (no cross-repo dependency since 2026-09-26).",
+    "The sandbox has 2 CPUs, ~4 GB RAM, no GPU: training the reference U-Net (ResNet-18) is out of scope here; the shipped model is HistGradientBoosting on sampled pixels.",
+    "Egress to the USGS 3DEP 1 m DEM bucket is blocked here, so the 1 m DEM link list (1m_DEM_links.csv) cannot be obtained directly from this host.",
+    "The 11-repo duplication is a real compliance exposure under rules A.12/A.16 until the other ten are archived.",
+    "Account holder must confirm eligibility (rules \u00a71.3: U.S. citizen/permanent resident, not Federal employee, not under 18) and finalise the generative-AI disclosure draft (rules \u00a73.2; see NARRATIVES.md)."
+  ]
+}
-- 
2.39.5

